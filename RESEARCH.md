# RESEARCH.md — 재고닥 착수 전 조사 4건

> **상태: 조사·설계만. 구현 대기.**
> **재고닥은 별도 레포·별도 Firebase 프로젝트다.** 루미 컬렉션과 통합하지 않는다.
> 루미(`lumi-admin`)는 지금 구조 그대로 두고, 재고닥은 처음부터 자기 스키마로 시작한다.
> 이 문서는 루미 코드를 **읽기만** 했다. 변경 0줄.

| 조사 | 내용 | 결과 |
| --- | --- | --- |
| **1** | 현재 단가 계산 방식 | **§A** — 이동평균 아님. **최종매입가 덮어쓰기.** ⚠️ 원가로 못 씀 + **단위 불일치**(§A-5(3)) |
| **2** | 주사제 믹스 원가 재사용성 | **§B** — 엔진은 훌륭함, **단 단가 원천이 §A라 같이 무너짐** |
| **3** | 매출 파서 입력 포맷 | **§C** — EMR 종속 확정. 재고닥 재사용 없음 |
| **4** | 명세서 사진 → 입고 자동입력 | **§D** — 설계 완료. v1 **8인일** |

관련 문서: `BRIEF.md` · `DECISIONS.md` / `DECISIONS-2.md` · `AUDIT.md` · `ISSUES.md` · `HANDOFF.md`

---

## 0. `DECISIONS-3.md` 회신 반영 (2026-08-30)

| 조사 | 회신 | 이 문서 반영 |
| --- | --- | --- |
| §A 단가 | **이동가중평균 채택.** `lastPrice` 병행 유지. §A-6 이관 매핑 그대로 승인 | §A-7 확정 |
| §A-5(1) | **최우선.** 루미의 **현재** 문제로 격상 — 리포트 스크립트 요청 | **`scripts/price-audit.js`** ✅ |
| §B 믹스 | v1 제외, **v1.5 이식.** 단 `items.volumeMl` + `supplies[].itemId` 는 v1부터 | §B-4 |
| §C 매출 파서 | 재사용 없음 확인. 좌표 엔진 3개는 **v2 폴백 때 이식** | §C-4 |
| §D OCR | **8인일 승인.** + packSize **하한 검사** 추가 요청 | **§D-3-5 (2)** ✅ |

**개발 순서도 바뀌었습니다** — 명세서 OCR이 **P2로 앞당겨졌습니다**(바코드와 자리 교체).
근거: TXA NIA 사고의 원인이 수기 입력이므로 재고닥의 첫 약속은 "수기 입력을 없앤다"가 되어야 하고,
바코드는 입고가 아니라 등록·개봉·실사에 쓰이기 때문입니다. (`DECISIONS-3.md` §3-5)

> **⚠️ 이 문서에서 아직 안 고친 것**: §D-9의 인일 배분은 P 순서 변경 전 기준입니다.
> 총량 8인일은 그대로지만, P2로 당겨지면서 **P1(품목·UOM)에 `packSize`·`trackMode`가
> 먼저 서 있어야 한다**는 선후관계가 생깁니다. P1 설계 시 §D-3을 같이 보십시오.

---

# §A. 조사 1 — 현재 단가 계산 방식

## A-1. 결론부터

> **이동평균이 아닙니다. 최종매입가(Last Purchase Price) 덮어쓰기입니다.**
> 게다가 **덮어쓰는 값 자체가 틀릴 수 있는 구조**입니다.
> `inventory.unitPrice`는 **"마지막에 산 가격"**이지 **"지금 갖고 있는 재고의 원가"**가 아닙니다.

`이동평균`·`가중평균`·`평균단가`·`avgCost`·`movingAvg` — **`staff.html`·`functions/index.js` 통틀어 0건.**
평균 원가 로직은 존재하지 않습니다.

## A-2. 계산식

```js
unitPrice = round(구매총액 / (박스수 × 박스당입수))
```

기존 재고량도, 기존 단가도 참조하지 않습니다. **이번 구매분만으로 계산해 통째로 덮어씁니다.**

## A-3. 쓰기 지점 4곳 — 서로 다르게 동작합니다

| # | 경로 | 위치 | `unitPrice` |
| --- | --- | --- | --- |
| 1 | 신규 품목 등록 | `staff.html:15459` | 새로 계산 |
| 2 | **재고 탭 📥입고** | `staff.html:15645~15650` | **덮어씀** + `priceHistory` 추가 |
| 3 | 품목 ✏️수정 저장 | `staff.html:16140` | **재계산해 덮어씀** |
| 4 | **입고 탭 「입고 완료」** | `staff.html:16852` | **건드리지 않음** ⚠️ |

> **⚠️ 입고 경로가 둘인데 단가 처리가 다릅니다.**
> 재고 탭으로 입고하면 단가가 갱신되고, 입고 탭으로 입고하면 배치(`batches[].purchasePrice`)에만 남고
> **품목 단가는 옛날 값 그대로입니다.** 같은 물건을 어느 탭으로 넣었느냐에 따라 단가가 달라집니다.
> (참고: `AUDIT-REPLY.md`에 적은 `receiveHistory` / `receivingHistory` 이원화와 같은 뿌리입니다.)

## A-4. 읽기 지점 — 폴백까지 동일

```js
// staff.html:8786  _invUnitPrice()  ·  staff.html:15091  _overviewItemUnitPrice()
unitPrice > 0 ? round(unitPrice) : (purchaseQty > 0 ? round(purchasePrice / purchaseQty) : 0)
```

이 값이 쓰이는 곳:

| 쓰는 곳 | 위치 | 의미 |
| --- | --- | --- |
| 재고 목록 「낱개당 단가」 | `staff.html:6810` | 화면 표시 |
| **재고 자산 평가** | `staff.html:15098` `_overviewItemValue = unitPrice × currentStock` | **재고 총액** |
| **주사제 믹스 원가** | `staff.html:3107` `_recipeResolve()` | **§B의 원천** |
| 시술 소모품 단가 자동입력 | `staff.html:3163` `_supplyLookup()` | 시술 원가 |

즉 **최종매입가 하나가 재고 자산 평가·믹스 원가·시술 원가를 전부 떠받치고 있습니다.**

## A-5. ⚠️ 발견한 구조적 위험 4건

### (1) 단가 분모와 재고 증가량이 서로 검증되지 않습니다 — 제일 위험

재고 탭 입고 화면에서 두 숫자를 **따로** 받습니다.

```js
// staff.html:15623~15627 — 단가의 분모
const totalQty = boxes * perBox;          // 폼의 「박스수 × 박스당 입수」, 기본값 1 × 1

// staff.html:15601~15607 — 실제 재고 증가량
addMap[loc] += qty;  totalAdd += qty;     // 장소별 수량 입력 행의 합
```

**두 값이 같은지 아무도 확인하지 않습니다.** 사용자가 박스수·입수를 기본값(1×1)으로 둔 채
「구매 총 금액 50,000」 + 「창고 100개」를 입력하면:

```
재고        +100 개
unitPrice   50,000 / (1×1) = 50,000원   ← 실제로는 500원. 100배
```

그리고 이 50,000원이 **재고 자산 평가에 100개 × 50,000 = 500만원**으로 잡힙니다.

> 코드상 가능하다는 것까지가 조사 범위입니다. **실제 데이터에 이런 품목이 있는지는 확인 못 했습니다**
> (프로덕션 스크립트를 돌리지 않는 규칙). 이관 전에 `unitPrice × purchaseQty ≠ purchasePrice`
> 인 품목을 세는 리포트를 돌려볼 것을 권합니다 — `scripts/fix-currentstock.js`와 같은 report 전용 형태로.

### (2) 가격과 무관한 수정에도 단가가 다시 써집니다

품목 ✏️수정 모달을 열면 구매정보를 **역산**해 채웁니다.

```js
// staff.html:16010~16013
document.getElementById('editItemBoxes').value = 1;
document.getElementById('editItemPerBox').value = item.purchaseQty || 1;
```

저장하면 `unitPrice`가 다시 계산됩니다(`staff.html:16140`). 보통은 같은 값이 나오지만,
**`purchasePrice`가 비어 있는 품목은 저장하는 순간 `unitPrice`가 0으로 덮입니다.**
카테고리만 고치려고 연 수정 모달이 단가를 지웁니다.

### ★ (3) 단위 불일치 — 산술 검산으로는 잡히지 않습니다

**루미 TXA NIA 가 이 경우입니다.** 실제 거래명세서(§D-1-4)로 확인했습니다.

```
구매      15박스(1박스 = 5병)에 2,200,000원   ← 유상 10 + 할증 5
루미 입력  구매총액 2,200,000 / 박스 1 × 박스당 15 → unitPrice 146,667
검산 ①    146,667 × 15 = 2,200,005 ≈ 2,200,000  ✔ 통과한다
```

**세 값은 완벽히 정합입니다. 146,667원도 계산이 맞습니다** — 할증까지 반영한 **박스당** 실취득가입니다.
틀린 것은 **재고 수량만 다른 단위로 세고 있다**는 점입니다.

```
currentStock 70 은 병(vial) 단위로 보입니다 (15박스 = 75병, 5병 사용 → 70병)
그런데 unitPrice 는 박스당

재고가치  70 × 146,667 = 10,266,690원
실제      70 ×  29,333 =  2,053,310원
                          ──────────
              과대분      8,213,380원   ← 5배 (= packSize)
```

**어떤 산술 검산으로도 못 잡습니다.** 단서는 하나뿐입니다 —
**재고 수량이 구매 수량보다 지나치게 많다**(70 vs 15 = 4.7배).
그리고 **그 배수가 곧 박스 입수에 가깝습니다**(4.7 ≈ 5) — 정답을 제안할 수 있습니다.

→ `scripts/price-audit.js` **검사 ⑥** 이 이 규칙으로 찾습니다. 실제 값을 넣어 보면
   「추정 입수 **5**, 보정 시 ₩2,053,310, 과대분 ₩8,213,380」 을 정확히 뽑아냅니다.

> ⚠️ **재고 70 의 단위는 실물을 세어 확인해야 합니다.** 병이면 위와 같고, 박스라면
> 15박스만 받았는데 70박스일 수 없으므로 다른 문제입니다. 이건 사람만 답할 수 있습니다.

**재고닥이 이 사고를 막는 방법**: 수량과 단가를 **같은 축**에 강제로 묶습니다.
`qtyPacks`/`qtyBase` 와 `unitPricePack`/`unitPriceBase` 를 항상 쌍으로 저장하고(§D-3),
입고 화면에서 `15박스 = 75병`을 같이 보여줍니다(§D-5-6). 사람이 단위를 고를 여지를 없앱니다.

### (4) `priceHistory`는 표시 전용입니다

`staff.html:15653~15664`가 최근 5개를 쌓지만, **읽는 곳은 `staff.html:6825`의 "이전 ₩X (거래처, 날짜) ↑↓차액" 한 줄뿐**입니다.
원가 계산에 쓰이지 않습니다. 평균을 내지도, 재고 소진에 매칭하지도 않습니다.

## A-6. 이관 스크립트에 미치는 영향 ★

**원장님 우려가 정확합니다.** `unitPrice`를 그대로 원가로 옮기면 안 됩니다.

| 루미 필드 | 재고닥으로 | 이유 |
| --- | --- | --- |
| `unitPrice` | → **`lastPrice`** (마지막 매입가) | 원가가 아니라 참고가. 이름을 바꿔서 오해를 끊는다 |
| `purchasePrice` / `purchaseQty` | → `lastPurchase: {amount, qty}` | 검산용 원본 보존 |
| `priceHistory[]` | → 그대로 이관 | 값은 살아 있다. 재고닥에서 활용도를 올릴 여지 |
| (원가) | **이관하지 않는다. 새로 쌓는다** | 신뢰할 만한 과거 원가가 존재하지 않음 |

**이관 시 반드시 할 것:**

1. **`unitPrice × purchaseQty ≠ purchasePrice` 인 품목을 전수 리포트**로 뽑아 원장님께 보여드린다.
   위 (1) 사고의 흔적이 여기서 드러납니다.
2. 이관 직후 재고닥의 원가는 **`lastPrice`를 초기값으로 쓰되 "추정"으로 표시**하고,
   첫 명세서 입고(§D)가 들어오는 순간 실측값으로 교체합니다.
3. **재고닥에서는 이동평균을 쓸 것인가?** — §A-7.

## A-7. 재고닥의 원가 방식 제안

| 방식 | 장점 | 단점 | 판단 |
| --- | --- | --- | --- |
| 최종매입가 (현행) | 단순. 계산 불필요 | 원가가 아님. 가격 급등락에 그대로 흔들림 | ✕ |
| **이동가중평균** | 재고 원가로 맞음. 입고마다 1줄 계산 | 재고 수량이 정확해야 함 | **○ 권장** |
| 선입선출(FIFO) | 가장 정확 | 배치 추적 필수. 소규모 병원엔 과함 | △ v2 |

**이동가중평균 권장.** 입고 1건마다 이 한 줄이면 됩니다.

```js
newAvg = (기존재고 × 기존평균 + 입고수량 × 입고단가) / (기존재고 + 입고수량)
```

- 명세서 OCR(§D)이 붙으면 **입고단가가 안분까지 반영된 실제 값**이라 평균의 품질이 좋습니다.
- 기존재고가 0이면 그냥 입고단가가 평균이 됩니다 — (1) 같은 사고가 평균에 희석되지 않고 즉시 보입니다.
- **`lastPrice`(마지막 매입가)도 같이 유지**합니다. 발주할 때 보는 건 평균이 아니라 마지막 가격입니다.

---

# §B. 조사 2 — 주사제 믹스 원가 재사용성

## B-1. 결론

> **엔진 자체는 예상보다 훨씬 잘 만들어져 있어서 재사용 가치가 높습니다.**
> **다만 단가 원천이 §A(최종매입가)라, §A를 안 고치면 믹스 원가도 같이 틀립니다.**
> 그리고 **시술↔재료 연결이 이름 문자열 기반**이라 SaaS에서 그대로 쓰면 깨집니다.

## B-2. 있는 것 — `injectionMixes`

`staff.html:3083~3170`. 컬렉션 `injectionMixes`.

```js
// injectionMixes/{mixId}
{
  name: '주사마취',
  recipe: [
    {itemId:'inv_...', itemName:'리도카인 2% 20ml', qty:1,   unit:'개'},
    {itemId:'inv_...', itemName:'생리식염수 100ml', qty:0.7, unit:'cc'},
  ],
  yieldUnit: 'cc',
}
```

| 기능 | 함수 | 위치 |
| --- | --- | --- |
| 믹스 원가 = Σ(재료 단가 × 수량) | `_mixCost()` | `staff.html:3121` |
| 총 생성량 = Σ(재료 용량) | `_mixYield()` | `staff.html:3147` |
| 단위단가 = 원가 ÷ 생성량 | `_mixUnitPrice()` | `staff.html:3158` |
| 개↔cc 자동 환산 | `_recipeLineCost()` | `staff.html:3112` |
| **믹스 중첩 참조** (믹스 안에 믹스) | `_recipeResolve()` | `staff.html:3104` |
| **순환 참조 차단** | `_mixCostStk` / `_mixYieldStk` | `staff.html:3120` / `3146` |
| 시술 소모품에서 믹스 선택 | `_supplyLookup()` | `staff.html:3163` |

**잘 만든 점:**

- 재고와 믹스를 **같은 공식**으로 처리합니다 — 재고는 `{낱개단가, 1개용량}`, 믹스는 `{배치원가, 총생성량}`.
  덕분에 믹스를 다른 믹스의 재료로 넣어도 코드 분기가 없습니다.
- **순환 참조를 Set 스택으로 막습니다.** 이건 보통 나중에 사고 나고서야 넣는 방어인데 이미 있습니다.
- 용량을 `item.volumeMl` → 저장값 → **이름에서 정규식 추정**(`"NS 100ml"` → 100) 3단으로 폴백합니다.

## B-3. ⚠️ 그대로 못 쓰는 것 3건

### (1) 단가 원천이 §A입니다 — 가장 큰 문제

```js
// staff.html:3107
if (it) return {up: _invUnitPrice(it), ...};   // ← §A-4의 최종매입가
```

**믹스 원가 = Σ(최종매입가 × 수량).** §A-5(1) 사고가 난 품목이 레시피에 하나라도 들어가면
믹스 원가가 통째로 틀리고, 그게 시술 원가로, 다시 손익으로 번집니다.
**§A를 이동가중평균으로 바꾸면 이 문제는 자동으로 해결됩니다.**

### (2) 시술↔재료 연결이 이름 문자열입니다

시술 수가표의 소모품 행 구조 (`staff.html:8854`):

```js
v.supplies.push({name:'', price:0, qty:1, unit:'개'});   // ← itemId 없음
```

**`itemId`가 없습니다.** 연결은 `_findInvByName()`(`staff.html:2901`)의 **이름 정확일치**뿐입니다.

```js
return (invItems||[]).find(i => String(i.name||'').trim() === s) || null;
```

품목 이름을 한 글자만 고치면 연결이 끊기고, 조용히 `price: 0`이 됩니다.
(믹스 레시피는 `itemId`를 갖고 있어 이보다 낫습니다 — `staff.html:3106`.)

### (3) 소모품 단가는 스냅샷이라 자동 갱신되지 않습니다

```js
// staff.html:8867 — 소모품을 "고를 때" 한 번만 복사
if (lk.price > 0) v.supplies[i].price = lk.price;
```

이후 재고 단가가 바뀌어도 **시술 수가표의 소모품 단가는 옛날 값 그대로**입니다.
재동기화 함수가 없습니다. 즉 시술 원가는 시간이 갈수록 실제와 벌어집니다.

### (4) 시술 수가표 전체가 문서 **하나**에 JSON 문자열로 들어 있습니다

```js
// staff.html:8086
db.collection('settings').doc('feeSchedule').set({ json: JSON.stringify(_feeData||[]), ... })
```

카테고리→시술→옵션→소모품 트리 **전체**가 `settings/feeSchedule` 한 문서의 `json` 필드입니다.

- Firestore 문서 상한 **1MB**. 시술이 늘면 언젠가 막힙니다.
- **부분 쿼리 불가** — "이 재료를 쓰는 시술" 을 찾으려면 전체를 파싱해야 합니다.
- **전체 덮어쓰기(last-write-wins)** — 두 사람이 동시에 편집하면 한쪽이 통째로 날아갑니다.

## B-4. 재고닥 손익 lite에 얹을 수 있나 — 판정

> **얹을 수 있습니다. 단 조건부이고, v1은 아닙니다.**

| 조건 | 필요 작업 |
| --- | --- |
| §A를 이동가중평균으로 교체 | §A-7 — **선행 필수** |
| `supplies[]`에 `itemId` 추가 | 이름 기반 → ID 기반. 이관 시 이름으로 1회 매칭 후 ID 고정 |
| 소모품 단가를 스냅샷 → **참조**로 | 표시할 때마다 현재 원가를 조회. 스냅샷은 "그때 얼마였나" 이력으로만 |
| 수가표를 문서 1개 → **컬렉션**으로 | `treatments/{id}` + `treatmentSupplies/{id}`. 1MB·동시편집 문제 동시 해결 |

**공수 개략 3~4인일** (믹스 엔진 자체는 이식만 하면 되므로 대부분 위 4건).

**권고: 재고닥 v1에서는 뺍니다.**
v1의 약속은 "재고가 맞는다"이지 "시술별 원가가 나온다"가 아닙니다.
다만 **`items`에 `volumeMl`(1개 용량) 필드는 v1부터 넣어 두십시오.** 이게 없으면 나중에
믹스를 얹을 때 전 품목을 다시 열어 용량을 채워야 합니다. 필드 하나 값은 나중에 채워도 됩니다.

---

# §C. 조사 3 — 매출 파서 입력 포맷

## C-1. 결론

> **파서가 둘 있고, 둘 다 루미가 쓰는 EMR에 종속입니다. 재고닥 v1에 재사용할 것은 없습니다.**
> 단 **표 파싱 하부 엔진(`dsAlign`/`dsAnchors`)만은 도메인 무관**이라 §D의 폴백으로 씁니다.

## C-2. 파서 ① `parseRevenueFile` — 엑셀 (Google Drive)

`functions/index.js:432`. 입력은 **사진이 아니라 EMR이 내보낸 엑셀 파일**입니다.

| 하드코딩 | 값 | 위치 |
| --- | --- | --- |
| Drive 폴더 ID | `1BLzrDhy8mvWUa27RsKlwhYV9sOYhG5iI` | `functions/index.js:372` |
| 파일명 규칙 | `오더판매내역및환자내역_YYYYMM.xlsx` | `functions/index.js:418` |
| 시트명 | `'오더별환자'` 포함 (없으면 2번째 시트) | `functions/index.js:454` |
| 헤더 위치 | 2행 헤더, 3행부터 데이터 (`range:1`) | `functions/index.js:457` |
| **컬럼 라벨 10개** | 차트번호·이름·진료의명·담당직원·코드·오더명·금액·국적·부가세·구분 | `functions/index.js:463~474` |

컬럼은 **정확일치**로 찾습니다(`String(h).trim() === label`). 한 글자만 달라도 못 찾습니다.
`금액` 컬럼이 없으면 즉시 실패합니다(`functions/index.js:475`).

**포맷에 박힌 도메인 규칙:**

```js
부가세 === 'O'          → 비보험(비급여)로 집계     // :519
진료의명 === '빠른예약'  → 진료의 집계에서 제외      // :529
금액 <= 0               → 행 스킵                  // :508
이름에 '합' 포함         → 합계 행으로 보고 제외      // :484
```

권한은 **대표원장 전용**입니다(`_checkAdminHigh`, `functions/index.js:437`).

## C-3. 파서 ② `ocrDailySales` — EMR 화면 사진

`functions/index.js:2161`. 입력은 **EMR 결산 화면을 찍은 사진**입니다. 표 3~4종을 인식합니다.

| 표 | 열 정의 | 위치 |
| --- | --- | --- |
| 수납 구분표 | `DS_PAY_COLS` 8열 + `DS_PAY_ROWS` **행 라벨 20개 한글 하드코딩** | `functions/index.js:1573` / `1585` |
| 담당직원별·담당의별 매출 | `DS_SALES_COLS` 11열 | `functions/index.js:1607` |
| 시술별 매출 | `DS_PROC_COLS` 11열 | `functions/index.js:1623` |

행 라벨이 `'현금현금영수증'`, `'미분류환불금미수처리'`, `'과세+비과세수납금액'` 처럼
**그 EMR의 문구 그대로** 들어 있습니다. 다른 EMR에서는 한 줄도 안 맞습니다.

## C-4. 재사용 판정

| 부분 | EMR 종속? | 재고닥 재사용 |
| --- | --- | --- |
| Drive 연동·파일명·시트·컬럼 (`parseRevenueFile`) | **완전 종속** | ✕ |
| 도메인 규칙 (부가세 O, 빠른예약 …) | **완전 종속** | ✕ |
| `DS_*_COLS` / `DS_PAY_ROWS` 라벨 | **완전 종속** | ✕ |
| `dsVerify*()` 검산 | 표 산식에 종속 | △ 사고방식만 |
| **`layoutWordRows()` 좌표·기울기 보정** | **무관** | **○** |
| **`dsRowNumbers()` 숫자 토큰 추출** | **무관** | **○** |
| **`dsAnchors()` / `dsAlign()` 열 정렬 DP** | **무관** | **○** |

**결론: 매출 파서는 재고닥에 안 가져갑니다.**
`HANDOFF.md §2-7`의 결정(매출·비용·세무는 `FINANCE_ENABLED`로 루미 전용 격리, SaaS 제외)이 그대로 유효하고,
이번 조사는 그 결정을 **더 강하게 지지**합니다 — 종속 지점이 예상보다 많습니다.

가져가는 건 **좌표 기반 표 읽기 엔진 3개뿐**이고, 그건 §D-3의 A안 폴백으로 쓰입니다.

---
# §D. 조사 4 — 명세서 사진 → 입고 자동입력 (설계)

> **v1 범위 확정: 약 8인일.** 초안의 14인일에서 "잘 되게 하는" 기능을 전부 뺐습니다(§D-9).
> **재고닥은 별도 레포·별도 Firebase**이므로 루미 컬렉션 통합 문제는 존재하지 않습니다.

| # | 항목 | 결정 |
| --- | --- | --- |
| 1 | 부가세 | **세포함 단가 하나.** 과세/면세 구분 v1 제외. 공급가액·세액은 `movements`에 **저장만** |
| 2 | OCR 방식 | **비전 LLM 1차 + 산식 검산.** 좌표 파서 폴백은 **v1 제외** |
| 3 | 품목 매칭 | **거래처 품목코드 정확일치 + 사람 선택.** 이름 유사도(2단)는 **v1 제외** |
| 4 | 할인·배송비·반품 | 품목별 세포함 금액 비율로 **안분**. **할증은 수량 안분**(§D-5-5) |
| 5 | 수량 단위 | **`qtyPacks` / `qtyBase` 둘 다 저장.** `bulk` 등급은 `sealedPacks += qtyPacks` |

---

## D-1. 표본 — (주)플랜닥스 거래명세표 2장

### D-1-1. 문서 구조

```
[머리글]  거래명세표 / 발행일
          ┌ 공급받는자 ┐        ┌ 공급자 ┐
          등록번호 / 상호(법인명) / 사업장주소 / 전화번호 / 합계금액
[표]      월 | 일 | 품목·규격 | 단위 | 수량 | 단가 | 공급가액 | 세액
[꼬리]    *** 이 하 여 백 ***  (필러)
          소계 : 수량 / 공급가액 / 세액
          합계금액
```

공급자 등록번호(플랜닥스 `199-81-02516`)를 **거래처 식별키**로 씁니다.
상호는 표기가 흔들리지만(㈜/주식회사/(주)) 사업자번호는 흔들리지 않습니다.

### D-1-2. 표에서 반드시 다뤄야 하는 것

| 현상 | 예 | 처리 |
| --- | --- | --- |
| **품목 셀이 2행** | 1행 `PRIME SYRINGE 일회용 주사기/5ml,23G×25mm/100개1박스`<br>2행 `[ 1000017590 ]` | 두 행을 한 품목으로 병합. 대괄호 안이 **거래처 품목코드** |
| **장식 접두어** | `⭐플닥추천⭐`, `🌸1개도 특가🌸` | 정규화에서 제거 |
| **규격이 이름에 붙음** | `/5ml,23G×25mm/100개1박스` | `/` 분리 → `spec`. `100개1박스`에서 **packSize 후보** 추출 |
| **필러 행** | `*** 이 하 여 백 ***`, 빈 행 | 스킵 |
| **할인 행** | `주문할인` 수량 1 · 단가 `-2,200` · 공급가액 `-2,000` · 세액 `-200` (①)<br>`-20,880` / `-18,982` / `-1,898` (②) | 품목 아님. **안분 대상**(§D-5) |
| **소계 행** | ① 16 / 95,368 / 9,532 → 합계 **104,900**<br>② 33 / 279,440 / 27,940 → 합계 **307,380** | **검산 기준값**(§D-7) |

### D-1-3. ★ 산식 검증 — 인쇄된 단가는 이미 세포함이다

```
6,880 × 3 = 20,640   ≠ 공급가액 18,764
                     = 공급가액 18,764 + 세액 1,876   ✔
3,100 × 1 =  3,100   ≠ 공급가액  2,819
                     = 공급가액  2,819 + 세액   281   ✔
```

**`단가 × 수량 = 공급가액 + 세액`** 이 성립합니다.
회계 문서의 통상 규칙(`단가 × 수량 = 공급가액`)과 다르고, 이 명세서는 **소비자가(세포함)를 단가 칸에 인쇄**합니다.

1. **"세포함 단가 하나로 간다"는 방침이 실제 서류와 정확히 일치합니다.**
   화면에서 부가세를 안 다뤄도 사람이 종이에서 읽는 숫자와 앱 숫자가 같습니다.
2. 파서는 이 관계를 **검산식**으로 씁니다. `|단가×수량 − (공급가액+세액)| ≤ 1` 이면 그 행은 맞은 것입니다.
3. **단 이건 플랜닥스 양식의 성질이지 보편 규칙이 아닙니다.** 다른 거래처는 세별도일 수 있습니다.
   → 두 가설을 세우고 문서 단위로 판정합니다(§D-4-2).

### D-1-4. 표본 ② — (주)프라이모리스 Order · **할증 구조**

두 번째 거래처 문서. 양식이 완전히 다른데(`Order`, 「증빙번호」, 열 이름도 다름) **두 가지가 확인됩니다.**

| No | 품목 | 규격 | 단위 | 수량 | 단가 | 금액 |
| ---: | --- | --- | --- | ---: | ---: | ---: |
| 1 | TXA NIA Sterile Transepidermal Concentra | `5x5ml` | ea | 10 | 220,000 | 2,200,000 |
| 2 | TXA FE AL Topical PIH Prevention | `30 ml` | ea | 10 | 132,000 | 1,320,000 |
| **3** | **TXA NIA** (1번과 같은 품목) | `5x5ml` | ea | **5** | **0** | **0** |
| **4** | **TXA FE AL** (2번과 같은 품목) | `30 ml` | ea | **5** | **0** | **0** |
| | 소계 | | | 30 | | 3,520,000 |
| | 공급가액 3,200,000 · 부가가치세 320,000 · **총계 3,520,000** | | | | | |

**(1) 세포함이 재확인됩니다.**

```
금액 합계        2,200,000 + 1,320,000 = 3,520,000
공급가액 + 세액  3,200,000 +   320,000 = 3,520,000   ✔ 일치
```

문서 하단에 **「부가세포함」**이라고 직접 인쇄돼 있습니다.
거래처가 다르고 양식이 다른데 §D-1-3 과 같은 결론입니다 — **「세포함 단가 하나」 방침이 옳습니다.**

**(2) ★ 할증(무상 제공) 행이 있습니다.** 이건 설계에 없던 케이스입니다.

3·4번은 **단가 0**입니다. 1·2번과 같은 품목이고, 새 품목이 아니라 **덤**입니다.
따라서 실제로 지불한 돈에 대응하는 수량은 **10개가 아니라 15개**입니다.

```
TXA NIA:  유상 10 + 할증 5 = 15박스에 2,200,000원
          인쇄 단가   220,000/박스   ← 이 값을 쓰면 33% 비싸게 잡힌다
          실취득 단가 146,667/박스   = 2,200,000 / 15
```

**할인이 「금액 조정」이라면 할증은 「수량 조정」입니다.** 방향은 같습니다 —
둘 다 인쇄된 단가가 실제 취득 단가가 아니라는 뜻이고, 재고닥은 **실제 값**을 저장해야 합니다.

---

## D-2. 부가세 (지시 ①)

### D-2-1. 확정

- **화면·입력·재고 계산은 전부 「세포함 단가」 하나로만.**
- **과세/면세 품목 구분은 v1에서 하지 않습니다.** 의료 소모품은 면세·과세가 섞이는데, 품목마다 붙이면
  소규모 병원 사용자가 못 채웁니다. 세무 신고는 세무사가 세금계산서로 합니다.
- **명세서에서 읽은 공급가액·세액은 저장만 하고 화면에 안 띄웁니다.**

### D-2-2. 저장 위치 — 재고닥은 처음부터 `movements` 하나

**루미의 `receiveHistory` / `receivingHistory` 이원화는 재고닥과 무관합니다.**
재고닥은 **별도 레포·별도 Firebase 프로젝트**이므로 루미 컬렉션을 통합하거나 마이그레이션할 일이 없습니다.
재고닥은 처음부터 `movements` 하나로 시작하고, 루미는 지금 구조 그대로 둡니다.

> 루미 → 재고닥 **이관 스크립트**에서만 두 컬렉션을 읽습니다. 읽기 전용이고, 루미는 변경하지 않습니다.
> (이관 시 단가를 어떻게 옮길지는 **§A-6** 참조 — 그게 이 조사에서 제일 중요한 결론입니다.)

### D-2-3. `movements` 스키마

```js
// movements/{movementId}          ※ 재고닥 프로젝트. 루미와 무관
{
  type: 'in',                 // in | out | adjust | return
  itemId: 'item_abc123',

  // ── 수량: 박스와 낱개를 둘 다 저장 (§D-3) ──
  qtyPacks: 3,                // 미개봉 박스 수 — 명세서 「수량」 그대로
  qtyBase:  300,              // 낱개 환산 = qtyPacks × packSize
  packSize: 100,              // ★ 이 입고 시점의 입수 (스냅샷)
  trackMode: 'bulk',          // bulk | unit — 이 품목의 등급 (스냅샷)

  // ── 단가: 박스당·낱개당 둘 다. 전부 세포함, 안분 반영 후 ──
  unitPricePack: 20216,       // 박스 1개당
  unitPriceBase: 202,         // 낱개 1개당 = unitPricePack / packSize
  amount: 60648,              // 이 행의 실제 금액 (안분 반영 후)

  // ── 명세서에서 읽은 원본. 저장만 하고 화면에 안 띄운다 ──
  tax: {
    source: 'ocr',            // ocr | manual | null
    supplyAmount: 55135,      // 공급가액
    vatAmount: 5513,          // 세액
    printedUnitPrice: 20640,  // 명세서 인쇄 단가 (안분 전, 박스당)
    vatIncluded: true,        // 인쇄 단가가 세포함인지 (§D-4-2 판정 결과)
  },

  allocation: {discount: -1272, shipping: 0, basis: 61920},   // §D-5
  purchaseDocId: 'pdoc_...',  // 원본 명세서
  vendorId: 'v_plandocs',
  createdAt, createdBy,
}
```

`tax`를 하위 객체로 묶는 이유: **화면 코드가 실수로 읽지 않게** 하려는 것입니다.
`unitPriceBase`만 보면 되는 UI가 `supplyAmount`를 집어 쓰는 사고를 구조로 막습니다.

---

## D-3. ★ 수량 단위 — `qtyPacks` / `qtyBase` (지시 추가)

### D-3-1. 문제

초안에 "명세서 수량 3이 박스 3개면 재고는 300개 증가"라고 썼는데, **SPEC의 박스 모델과 안 맞습니다.**
`bulk` 등급은 낱개를 세지 않고 미개봉 박스 수(`sealedPacks`)로 관리하기로 했으므로,
명세서 수량 3은 `sealedPacks += 3`이지 `totalBase += 300`이 아닙니다.

### D-3-2. 제안 — 둘 다 저장하고, 재고 반영만 등급별로 분기

```js
// 파서가 읽은 것
명세서 수량 = 3        →  qtyPacks = 3
packSize    = 100      →  qtyBase  = 300      // 항상 같이 계산해 둔다

// 재고 반영은 등급에 따라 분기
if (item.trackMode === 'bulk') {
  item.sealedPacks += qtyPacks;               // ★ 3박스. 낱개는 세지 않는다
  // qtyBase 는 movements 에만 남는다 — 금액 환산·통계용 참고값
} else {
  item.totalBase   += qtyBase;                // 300개
}
```

**둘 다 저장하는 이유:**

| 필요 | 쓰는 값 |
| --- | --- |
| `bulk` 재고 증감 | `qtyPacks` |
| `unit` 재고 증감 | `qtyBase` |
| **낱개당 단가 비교** (거래처 A 박스100 vs B 박스50) | `qtyBase` — 이게 없으면 비교 불가 |
| 소진 속도·발주 예측 | `qtyBase` (등급 무관하게 같은 축으로) |
| **등급 전환** (`bulk` ↔ `unit`) | 둘 다 — 과거 이력을 다시 계산하지 않아도 됨 |
| 명세서와 대조 | `qtyPacks` (인쇄된 숫자 그대로) |

### D-3-3. `packSize`는 스냅샷이어야 합니다

`movements.packSize`에 **그때의 입수를 복사해 둡니다.** `items.packSize`를 참조하면 안 됩니다.

거래처가 박스 입수를 100개 → 50개로 바꾸면, `items.packSize`만 보는 구조에서는
**과거 movement 전부가 다시 해석되어 예전 입고 수량이 절반으로 바뀝니다.** 스냅샷이면 과거는 과거대로 남습니다.
`trackMode`도 같은 이유로 스냅샷입니다.

### D-3-4. 단가도 두 축

```js
unitPricePack = round(안분 반영 후 행 금액 / qtyPacks)
unitPriceBase = round(unitPricePack / packSize)
```

**안분(§D-5)은 `unitPricePack` 기준 금액에 적용하고, `unitPriceBase`는 거기서 재산출**합니다.
반대로 하면 낱개 반올림 오차가 박스 수만큼 증폭됩니다.

### D-3-5. packSize 오류 안전장치 — 상한·하한 양방향

`packSize`는 **양쪽으로** 틀릴 수 있고, 두 사고는 성격이 다릅니다.

| 방향 | 예 | 결과 |
| --- | --- | --- |
| **상한** — packSize가 너무 큼 | 실제 낱개 3개인데 `×100`으로 봄 | 재고 **300개**로 부풀음 |
| **하한** — packSize가 너무 작음 | 실제 300개 들어왔는데 `packSize=1`이라 **3개**로 기록 | 재고 **부족**. 조용히 품절 |

#### (1) 상한 검사 — 사용량 대조 (§D-10 4번 답)

> **입고 후 재고가 「평균 월 사용량 × 12개월」을 넘으면 확인을 요구한다.**

배수 기준(예: 5배)은 원래 재고가 0이면 무의미해서 채택하지 않습니다.

```js
const monthly = avgMonthlyUsage(itemId);        // 최근 6개월 출고 기준
if (monthly > 0 && newStock > monthly * 12) {
  warn(`1년치(${monthly*12})를 넘습니다. 박스 입수(${packSize})가 맞나요?`);
}
```

**⚠️ 사용 이력이 없는 품목에는 안 돕니다** (`monthly === 0`).

#### (2) ★ 하한 검사 — 금액 대조 (`DECISIONS-3.md` §3-4 요청)

**명세서 금액이 정답지입니다.** 이게 (1)보다 강력한 이유는 **사용 이력이 없어도 돌기 때문**입니다.

```js
// 이 행이 실제로 얼마짜리 낱개인지, 금액에서 역산한다
const impliedBase = lineGross / qtyBase;        // qtyBase = qtyPacks × packSize
const known       = item.avgCostBase || item.lastPriceBase;   // 우리가 아는 낱개단가
if (!known) return needsHumanCheck();           // 첫 입고 — (3) 으로

const ratio = impliedBase / known;

if (ratio >= 5)      block(`낱개단가가 아는 값의 ${ratio.toFixed(0)}배입니다. `
                         + `박스 입수가 ${packSize}가 아니라 `
                         + `${Math.round(packSize * ratio)}이(가) 아닐까요?`);   // 하한 사고
else if (ratio <= 0.2) block(`낱개단가가 아는 값의 1/${(1/ratio).toFixed(0)}입니다. `
                         + `박스 입수를 확인하세요.`);                            // 상한 사고
```

**이 식의 좋은 점: `ratio`가 곧 틀린 배수라서 정답을 제안할 수 있습니다.**
packSize가 100이어야 하는데 1로 들어갔다면 `qtyBase`가 100분의 1이 되고,
`impliedBase`는 정확히 100배로 뜹니다 → `제안 packSize = round(1 × 100) = 100`.
"뭔가 이상합니다"가 아니라 **"100이 아닐까요?"**라고 물을 수 있습니다.

임계값 5배/0.2배는 **가격 변동을 오탐하지 않을 만큼 넓게** 잡은 것입니다.
실제 단가가 5배 오르는 일은 거의 없고, 있더라도 확인 한 번 받는 게 맞습니다.

#### (3) 이력이 아예 없는 첫 입고 — 사람 확인 필수

(1)도 (2)도 못 도는 유일한 구간이고, **여기가 제일 위험합니다.** 세 가지 힌트를 모아 제시합니다.

| 힌트 | 예 | 신뢰도 |
| --- | --- | --- |
| 품목명에서 추출 | `.../100개1박스` → `100` | 높음 |
| 명세서 「단위」 칸 | `BOX`/`박스`/`CS` 인데 `packSize === 1` → **모순** | 높음 |
| 명세서 「규격」 | `100T`, `25EA` | 중간 |

힌트가 하나라도 잡히면 **그 값을 기본값으로 채워** 보여주고, 사람은 확인만 합니다.
아무 힌트도 없으면 빈칸으로 두고 **입력 전에는 저장을 막습니다.**

#### (4) 문서 전체 금액 대조 — 마지막 방어선

행별 검사를 다 통과해도, **문서 합계가 안 맞으면 저장을 차단**합니다
(`DECISIONS-3.md` §3-5 「휴먼에러 방어 3겹」의 3번). 이건 §D-7 문서층 검산이 그대로 합니다.
루미는 대조할 정답지가 없어 사고가 났지만(§A-5(1)), 재고닥은 명세서 총액을 갖고 있으므로
**구조적으로 막을 수 있습니다.**

---

## D-4. 양식 비종속 파이프라인

```
① 전처리      리사이즈(장변 1600~2000px) / JPEG 0.85~0.92
② 텍스트화    비전 LLM에 이미지 투입
③ 머리글      거래처(사업자번호·상호), 발행일, docKind
④ 표          헤더 탐지 → 열 매핑 → 데이터 행 범위
⑤ 행 조립     2행 셀 병합 / 필러·소계 제외 / 품목 vs 조정행 분류
⑥ 검산        §D-7
⑦ 안분        §D-5
⑧ 품목 매칭   §D-6
⑨ 확인 화면   사람이 보고 고친 뒤 저장  ← 생략 불가
```

### D-4-1. 열 동의어 사전

거래처마다 열 이름이 다릅니다. **열을 이름이 아니라 「의미」로 잡습니다.**

```js
const COLUMN_SYNONYMS = {
  date:         ['월','일','일자','날짜','거래일','납품일'],
  name:         ['품목','품명','품목명','상품명','제품명','품목/규격','내역','적요'],
  spec:         ['규격','사양','SPEC','표준'],
  vendorCode:   ['코드','품목코드','상품코드','제품코드','CODE','바코드'],
  unit:         ['단위','UNIT','수량단위'],
  qty:          ['수량','납품수량','출고수량','개수','QTY'],
  unitPrice:    ['단가','판매단가','납품단가','단가(원)','소비자가'],
  supplyAmount: ['공급가액','공급가','금액','공급대가','과세표준'],
  vatAmount:    ['세액','부가세','부가가치세','VAT'],
  amount:       ['합계','합계금액','총액','금액계'],
};
```

비전 LLM에서는 이 사전을 **프롬프트에 넣어 출력 필드를 고정**하는 용도로 씁니다.
(좌표 파서에서는 헤더 행 탐지에 쓰이지만, 그건 v1 범위 밖입니다.)

### D-4-2. 세포함/세별도 판정

문서 단위로 판정합니다.

```js
hypoIncluded = Σ | qty*unitPrice − (supply + vat) |
hypoExcluded = Σ | qty*unitPrice −  supply        |
vatIncluded  = hypoIncluded < hypoExcluded
```

- 플랜닥스 2장 → `vatIncluded = true` (§D-1-3).
- 공급가액·세액 열이 아예 없으면 판정 불가 → **인쇄 단가를 그대로 세포함으로 받습니다.**
  우리 방침이 세포함 하나이므로 그게 안전한 기본값입니다.
- 결과는 `movements.tax.vatIncluded`에 남깁니다.

---

## D-5. 할인·배송비·반품 안분 (지시 ④)

### D-5-1. 규칙

명세서 꼬리의 금액 행(`주문할인`, `배송비`, `반품`)은 **품목이 아닙니다.**
**각 품목의 세포함 금액 비율로 안분해 단가에 반영합니다.**

재고닥의 단가는 **"이 물건이 실제로 얼마에 들어왔나"** 여야 합니다.
할인을 별도 행으로 남기면 품목 단가가 실제보다 비싸게 잡히고, 발주 예측·소진 단가가 전부 틀어집니다.

### D-5-2. 알고리즘

```js
function allocate(lines, adjustments) {
  // lines[i].gross = 세포함 금액 (= printedUnitPrice * qtyPacks)
  const basis = lines.reduce((s, l) => s + l.gross, 0);
  if (basis <= 0) return lines.map(l => ({...l, alloc: 0}));

  for (const adj of adjustments) {          // 할인·반품 음수, 배송비 양수
    let acc = 0;
    lines.forEach((l, i) => {
      const share = (i < lines.length - 1)
        ? Math.round(adj.amount * l.gross / basis)
        : adj.amount - acc;                 // ★ 잔차는 마지막 행에
      if (i < lines.length - 1) acc += share;
      l.alloc = (l.alloc || 0) + share;
    });
  }

  return lines.map(l => {
    const net = l.gross + (l.alloc || 0);
    const pack = Math.round(net / l.qtyPacks);
    return {...l,
      amount: net,
      unitPricePack: pack,                          // §D-3-4
      unitPriceBase: Math.round(pack / l.packSize),
      allocation: {discount: l.alloc, basis: l.gross},
    };
  });
}
```

**잔차 처리가 중요합니다.** 비율 배분은 원 단위 반올림 때문에 합이 안 맞습니다.
마지막 행에 잔차를 몰면 **Σ안분 = 조정액**이 정확히 성립하고 소계 검산(§D-7)이 통과합니다.

### D-5-3. 실제 숫자로 계산 — 명세서 ①

합계 **104,900**, 주문할인 **-2,200**(세포함).
할인이 합계에 이미 반영돼 있으므로 **할인 전 품목 세포함 총액 = 104,900 + 2,200 = 107,100**.

| 행 | 세포함(gross) | 안분 계산 | 안분액 | 반영 후 금액 | 반영 후 단가 |
| --- | ---: | --- | ---: | ---: | ---: |
| `6,880 × 3` | 20,640 | −2,200 × 20,640 / 107,100 | **−424** | 20,216 | **6,739** |
| `3,100 × 1` | 3,100 | −2,200 × 3,100 / 107,100 | **−64** | 3,036 | **3,036** |
| (나머지 행) | 83,360 | 나머지 | −1,712 | 81,648 | … |
| **계** | **107,100** | | **−2,200** | **104,900** ✔ | |

> 나머지 행은 옮겨 적지 않았습니다(확인용 두 줄 발췌). 잔차 −1,712는 총액 정합을 보인 것입니다.

**`6,880`짜리로 보이던 물건이 실제로는 6,739원에 들어왔습니다.** 이 차이가 원가와 발주 예측에 그대로 들어갑니다.

### D-5-4. 반품 (§D-10 5번 답)

> **기본은 안분하지 않습니다.** 확인 화면에서 사람이 품목을 지정하게 하고, **지정 안 하면 그때 안분**합니다.

할인·배송비는 거래 전체의 금액 조정이라 안분이 맞지만, 반품은 대개 **특정 품목의 수량 반환**입니다.
안분하면 엉뚱한 품목 단가가 오염됩니다.

```
adjustments[].kind === 'return'
 ├ 사람이 품목 지정  → 그 품목에 type:'return', qtyPacks 음수로 별도 movement
 └ 지정 안 함        → 할인과 동일하게 안분
```

파서는 `targetLineIndex`(추정)를 같이 넘기고, 확인 화면에서 사람이 바꿉니다.

### D-5-5. ★ 할증(무상 제공) — 금액이 아니라 **수량**을 안분한다

`DECISIONS-3` 이후 실제 명세서(§D-1-4)에서 발견한 케이스입니다. 할인과 **방향이 반대**입니다.

| | 조정 대상 | 실취득 단가 |
| --- | --- | --- |
| 할인·배송비 | **금액** | `(지불액 ± 조정액) / 수량` |
| **할증** | **수량** | `지불액 / (유상수량 + 무상수량)` |

```js
// 단가 0(또는 금액 0)인 행은 새 품목이 아니라 같은 품목의 덤이다.
// 품목 식별자(거래처 코드 우선, 없으면 정규화한 이름+규격)로 유상 행에 합친다.
function mergeFreeGoods(lines) {
  const byKey = new Map();
  for (const l of lines) {
    const key = l.vendorCode || normalizeName(l.name) + '|' + normalizeSpec(l.spec);
    const paid = (Number(l.unitPrice) || 0) > 0 && (Number(l.amount) || 0) > 0;
    const cur = byKey.get(key) || {...l, qtyPacksPaid: 0, qtyPacksFree: 0, gross: 0};
    if (paid) { cur.qtyPacksPaid += l.qty; cur.gross += l.amount; }
    else      { cur.qtyPacksFree += l.qty; }          // ★ 금액은 0. 수량만 더한다
    byKey.set(key, cur);
  }
  return [...byKey.values()].map((l) => ({
    ...l,
    qtyPacks: l.qtyPacksPaid + l.qtyPacksFree,        // 재고에 들어가는 수량
    // 단가는 §D-5-2 안분을 거친 뒤 이 수량으로 나눈다
  }));
}
```

**순서가 중요합니다: 할증 병합 → 금액 안분 → 단가 산출.**
할증을 먼저 합쳐야 안분 기준(`gross`)이 유상 금액만으로 잡힙니다.

**검산에 미치는 영향** (§D-7):
- 열 검산의 `Σ qtyPacks == 소계 수량` 은 **할증 포함**이어야 맞습니다 (표본 ②의 소계 30 = 10+10+5+5).
- 행 검산은 단가 0 행을 **건너뜁니다** — `0 × 5 = 0` 은 늘 참이라 아무것도 검증하지 못합니다.

**⚠️ 위험**: 할증 행을 못 알아보고 별도 품목으로 등록하면 **같은 품목이 둘**이 됩니다.
그래서 확인 화면에서 `유상 10 + 할증 5 = 15`를 **한 줄로 묶어** 보여줘야 합니다(§D-8).

### D-5-6. ★ 입고 입력 흐름 — 직원이 헷갈리지 않게

원장 지적: *"나도 헷갈리는데 직원들은 당연히 헷갈릴 듯."* 맞습니다.
지금 루미 화면은 **「박스 수 × 박스당 낱개」를 직원이 스스로 해석해서 채우는 구조**라
같은 물건을 사람마다 다르게 넣습니다. 재고닥은 **명세서에 인쇄된 것만 그대로 옮기게** 합니다.

```
┌ 입고 ─────────────────────────────────────────────┐
│ 품목   TXA NIA                                    │
│                                                   │
│ 받은 수량   유상 [ 10 ] 박스                       │
│             할증 [  5 ] 박스     ← 덤. 없으면 0    │
│             ────────────────                      │
│             합계  15 박스 = 75 병                  │
│                                                   │
│ 지불 금액   [ 2,200,000 ] 원   (세포함)            │
│                                                   │
│ ── 자동 계산 (직원이 손대지 않음) ──               │
│    박스당  146,667원                              │
│    병당     29,333원                              │
│    ※ 명세서 인쇄 단가 220,000원 → 할증 반영 후     │
└───────────────────────────────────────────────────┘
```

**설계 원칙 셋:**

1. **직원은 명세서에 「있는 것」만 입력합니다** — 수량과 금액. **단가는 계산 결과이지 입력값이 아닙니다.**
   루미 사고(§A-5(1))의 뿌리가 단가를 사람이 정하게 둔 것입니다.
2. **박스와 낱개를 항상 같이 보여줍니다** — `15박스 = 75병`. 여기서 packSize 오류가 눈에 띕니다(§D-3-5).
3. **입수(packSize)는 품목에 미리 등록**해 두고 입고 때는 확인만 합니다. 매번 물으면 매번 다르게 답합니다.

**P2(명세서 OCR)가 붙으면 이 화면은 그대로 「확인 화면」이 됩니다** — 숫자가 미리 채워진 채로.
즉 지금 이 흐름을 P1에서 제대로 세워 두면 P2는 채우기만 하면 됩니다.

---

## D-6. 품목 매칭 (지시 ③)

### D-6-1. `items.vendorCodes`

```js
// items/{itemId}
{
  name: '일회용 주사기 5ml',
  trackMode: 'bulk',             // bulk | unit
  packSize: 100,                 // 우리 기본 입수
  volumeMl: null,                // 1개 용량 — §B-4. v1은 비워 둬도 됨
  ...
  vendorCodes: [
    {
      vendorId:   'v_plandocs',
      code:       '1000017590',  // ★ 매칭 키
      vendorName: 'PRIME SYRINGE 일회용 주사기/5ml,23G×25mm/100개1박스',  // 명세서 원문
      packSize:   100,           // 이 거래처가 1줄로 파는 단위
      lastPrice:  6739,          // 마지막 낱개 단가 (세포함·안분 반영 후)
      lastAt:     '2026-08-21',
      confirmedBy:'김OO',
    },
  ],
  vendorCodeKeys: ['v_plandocs::1000017590'],   // array-contains 조회용 평탄화 키
}
```

- **품목 하나에 거래처별 코드가 여럿** 붙습니다.
- `packSize`가 거래처별로 다릅니다 — A는 박스 100개, B는 50개. `vendorCodes[].packSize`가 정답이고
  `items.packSize`는 기본값입니다.
- `lastPrice`는 **낱개당**(`unitPriceBase`)을 넣습니다. 거래처 간 비교가 되려면 낱개축이어야 합니다.
- `vendorCodes`는 배열이라 Firestore가 객체 부분일치를 못 찾습니다 → `vendorCodeKeys`로 1회 조회.

### D-6-2. v1 매칭 — 2단만

```
[1단] 거래처 품목코드 정확일치 ──→ 자동 매칭
      key = `${vendorId}::${normalizeCode(code)}`
      normalizeCode: 공백·대괄호 제거, 대문자화, 앞 0 유지
        '[ 1000017590 ]' → '1000017590'

[2단] 못 찾으면 ──→ 사람이 목록에서 고른다
      [기존 품목에 연결] / [신규 품목으로 등록] / [이번엔 건너뜀]
```

> **이름 유사도 자동 후보 제시는 v1에서 뺍니다**(§D-9).
> 코드가 안 찍히는 거래처에서는 사람이 매번 고르게 되지만, **한 번 고르면 `vendorCodes`에 자동 등록**되어
> 두 번째 명세서부터 1단으로 승격됩니다. 즉 손이 가는 건 거래처당 초기 몇 번뿐입니다.
> 이게 실제로 얼마나 번거로운지는 파일럿에서 **사람 수정 클릭 수**로 재고, 그때 필요하면 넣습니다.

### D-6-3. 미매칭 처리

- **[건너뜀]을 반드시 둡니다.** 명세서에 재고 관리 대상이 아닌 것(사은품, 판촉물)이 섞입니다.
  건너뛴 코드는 `vendorCodeIgnored`에 남겨 다음부터 안 묻습니다.
- **[신규 등록]은 기본값을 명세서에서 채웁니다** — 이름·규격·단위·packSize·단가·거래처·코드.
  사용자는 **등급(bulk/unit)·카테고리·보관 장소**만 고르면 됩니다.
  **이때 packSize를 반드시 확인시킵니다**(§D-3-5).

---

## D-7. 검산 — OCR은 반드시 틀린다는 전제

비전 LLM은 못 읽은 칸을 빈칸으로 두지 않고 **그럴듯한 숫자를 만들어 낼 수 있습니다.**
재고·금액 데이터에서 이건 조용한 오염이라, 검산이 선택이 아니라 필수입니다.
명세서는 검산식이 **세 겹**으로 있어서 유리합니다.

| 층 | 식 | 깨지면 |
| --- | --- | --- |
| **행** | `qtyPacks × printedUnitPrice == supply + vat` (세포함) 또는 `== supply` (세별도) | 그 행만 빨갛게 |
| **열** | `Σ qtyPacks == 소계 수량`, `Σ supply == 소계 공급가액`, `Σ vat == 소계 세액` | 행 누락/환각 의심 |
| **문서** | `소계 공급가액 + 소계 세액 == 합계금액`, `Σ gross + Σ adjustments == 합계금액` | 조정행 누락 의심 |

허용 오차: 행별 ±1원, 문서 합계 ±(행 수)원.

**세 층이 모두 통과** → 환각 확률 매우 낮음 → 자동 채움, 사람은 훑어보기.
**한 층이라도 깨짐** → **전 행 사람 확인 모드**로 떨어뜨립니다. (좌표 파서 대조군은 v1 제외 — §D-9.)

---

## D-8. 확인·수정 화면

```
┌ 명세서 사진 (핀치 줌) ┬ 파싱 결과 ─────────────────────────────┐
│                        │ ✅ 일회용 주사기 5ml                    │
│   (원본 이미지)        │    3박스 (×100 = 300개)  bulk          │
│                        │    박스당 20,216 / 낱개 202            │
│                        │    [플랜닥스 1000017590]               │
│                        │ ⚠️ 알콜솜  1  → 매칭 안 됨              │
│                        │    [연결] [신규 등록] [건너뜀]          │
│                        │ ⛔ 3행: 단가×수량 ≠ 공급가액+세액        │
│                        │────────────────────────────────────────│
│                        │ 주문할인 −2,200 → 전 품목에 안분됨      │
│                        │ 소계 ✔  합계 104,900 ✔                 │
└────────────────────────┴────────────────────────────────────────┘
             [모두 확인했습니다 — 입고 처리]
```

- **원본 사진을 옆에 붙입니다.** 대조할 수 없으면 확인 화면이 아니라 승인 버튼일 뿐입니다.
- **박스와 낱개를 같이 보여줍니다** — `3박스 (×100 = 300개)`. packSize 오류가 여기서 눈에 띕니다.
- **어디가 미심쩍은지 색으로 구분**합니다. 정확도보다 "틀린 곳을 찾는 시간"이 실제 사용성입니다.
- **보관 장소**는 명세서에 없으므로 반드시 사람이 고릅니다. 품목별 마지막 장소를 기본값으로.
- 저장은 **한 번에 트랜잭션**으로. 부분 저장되면 재고가 이상해집니다.

---

## D-9. v1 범위 — 14인일 → 8인일

**뺀 것들은 전부 "잘 되게 하는" 기능이지 "되게 하는" 기능이 아닙니다.**
파일럿에서 **사람 수정 클릭 수**를 재보고 필요하면 그때 넣습니다.

| 항목 | 초안 | v1 | 판단 |
| --- | ---: | ---: | --- |
| 골든셋 구축 + 실측 | 2.0 | **0.5** | 20~30장 → **5장**. 거래처 5곳 20~30장은 파일럿 이후 |
| 비전 LLM 프롬프트·스키마·튜닝 | 2.0 | 2.0 | 유지 |
| 머리글/열 동의어 사전 | 1.5 | **0.5** | 벤더 프로파일 캐시 제외. B안은 양식 비종속이 공짜 |
| 검산 3층 | 1.0 | 1.0 | 유지 — **환각 방어라 못 뺍니다** |
| 안분 | 0.5 | 0.5 | 유지 |
| 품목 매칭 | 2.0 | **1.0** | **이름 유사도(2단) 제외.** 코드 정확일치 + 사람 선택 |
| 확인·수정 화면 | 2.5 | 2.0 | 유지 (박스/낱개 표시 추가, 대조군 패널 제외) |
| 저장 트랜잭션 + `movements` | 1.0 | 0.5 | 재고닥 자체 스키마라 통합 작업이 없음 |
| **A안(좌표 파서) 폴백 결선** | 1.5 | **0** | **제외.** 검산 실패 시 전 행 수기 모드로 |
| **벤더 프로파일 캐시** | 1.5 | **0** | **제외** |
| **합계** | **14.0** | **8.0** | |

**뺀 것을 나중에 넣을 때의 순서** (파일럿 결과에 따라):
① 이름 유사도 매칭 → ② 벤더 프로파일 캐시 → ③ 골든셋 확대 → ④ 좌표 파서 폴백.
①이 제일 효과가 클 것으로 봅니다 — 코드가 안 찍히는 거래처에서 클릭이 가장 많이 발생합니다.

---

## D-10. 미결 7건 — 답 반영 완료

| # | 질문 | 답 | 반영 |
| --- | --- | --- | --- |
| 1 | 벤더 프로파일 병원 간 공유 | **v1은 병원별.** 오확정 전파 위험 > 이득. 게다가 프로파일 자체가 v1 제외라 **질문 소멸** | §D-9 |
| 2 | 원본 이미지 보관 | **보관.** 장당 200KB × 월 50장 × 100병원 ≈ 월 1GB 수준이라 비용이 문제가 아님. 없으면 "이 단가 왜 이래?" 추적 불가. **보관기간: 구독 종료 후 30일** | §D-8, §D-11 |
| 3 | 비전 LLM 벤더 | **실측 후.** 비용은 결정 요인이 아니므로 **한국어 표 인식 성능만** 본다 | §D-9 |
| 4 | packSize 오류 안전장치 | **상한** = 재고 > 평균 월사용량 × 12개월. **하한** = 명세서 금액 역산 대조(`ratio`가 곧 틀린 배수라 정답을 제안할 수 있음). 이력 없는 첫 입고는 사람 확인 | **§D-3-5** |
| 5 | 반품 품목 특정 | **기본 안분 안 함.** 사람이 품목 지정, 지정 없으면 안분 | **§D-5-4** |
| 6 | `receiveHistory`/`receivingHistory` 통합 시점 | **소멸.** 재고닥은 별도 레포·별도 Firebase. 루미 컬렉션 통합 안 함 | **§D-2-2** |
| 7 | 세금계산서·영수증 통합 | **v1은 거래명세서만.** `docKind` 필드만 유지 | §D-2-3 |

## D-11. 남은 확인 사항

| # | 내용 |
| --- | --- |
| 1 | **원본 이미지 보관 위치·삭제 잡** — 구독 종료 +30일 삭제를 누가 돌리나(스케줄 함수). 탈퇴 시 즉시삭제 요청 처리 |
| 2 | **비전 LLM 처리위탁 고지** — 명세서에 병원 상호·사업자번호·주소·전화가 찍혀 있음. 진료정보는 아니지만 개인정보 처리방침에 위탁 명시 필요 |
| 3 | **골든셋 5장 확보** — 플랜닥스 2장은 있음. 다른 거래처 3장 필요. 원장님께 요청 |

---

# §E. 참조

| 대상 | 위치 |
| --- | --- |
| **[조사1]** 낱개단가 계산 (읽기) | `staff.html:8786` `_invUnitPrice()` / `:15091` `_overviewItemUnitPrice()` |
| **[조사1]** 단가 쓰기 — 신규 등록 | `staff.html:15459` |
| **[조사1]** 단가 쓰기 — 재고 탭 입고 | `staff.html:15645` (덮어쓰기) |
| **[조사1]** 단가 쓰기 — 품목 수정 | `staff.html:16140` (재계산) |
| **[조사1]** 입고 탭 — 단가 미갱신 | `staff.html:16852` `submitReceiving()` |
| **[조사1]** 재고 자산 평가 | `staff.html:15098` `_overviewItemValue()` |
| **[조사1]** 가격 이력 (표시 전용) | `staff.html:15653` `priceHistory` |
| **[조사2]** 믹스 로드 | `staff.html:3087` `injectionMixes` |
| **[조사2]** 믹스 원가 / 생성량 / 단위단가 | `staff.html:3121` / `:3147` / `:3158` |
| **[조사2]** 재료 해석 (재고↔믹스 공통) | `staff.html:3104` `_recipeResolve()` |
| **[조사2]** 소모품 단가 조회 | `staff.html:3163` `_supplyLookup()` |
| **[조사2]** 소모품 행 구조 (itemId 없음) | `staff.html:8854` |
| **[조사2]** 단가 스냅샷 (재동기화 없음) | `staff.html:8867` |
| **[조사2]** 이름 기반 재고 조회 | `staff.html:2901` `_findInvByName()` |
| **[조사2]** 수가표 = 문서 1개 JSON | `staff.html:8086` `settings/feeSchedule` |
| **[조사3]** 엑셀 매출 파서 | `functions/index.js:432` `parseRevenueFile` |
| **[조사3]** Drive 폴더 ID 하드코딩 | `functions/index.js:372` |
| **[조사3]** 시트·헤더·컬럼 라벨 | `functions/index.js:454` / `:457` / `:462` |
| **[조사3]** 사진 매출 파서 | `functions/index.js:2161` `ocrDailySales` |
| **[조사3]** 열·행 라벨 하드코딩 | `functions/index.js:1573` / `:1585` / `:1607` / `:1623` |
| **[조사4]** 좌표·기울기 보정 (재사용 가능) | `functions/index.js:787` `layoutWordRows()` |
| **[조사4]** 숫자 토큰 추출 | `functions/index.js:1670` `dsRowNumbers()` |
| **[조사4]** 열 정렬 DP / 기준선 | `functions/index.js:1702` `dsAlign()` / `:1738` `dsAnchors()` |
| **[조사4]** Vision 호출 예시 | `functions/index.js:1350` `ocrReceipt` |
