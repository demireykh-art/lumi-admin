# RESEARCH.md — 명세서 사진 → 입고 자동입력 (설계)

> **상태: 설계만. 구현 대기.**
> 착수 시점 미정. 이 문서는 결정된 방침과 설계를 적어 두는 곳이고, 코드는 한 줄도 쓰지 않았다.
> 대상은 **재고닥(SaaS)** 이다. 루미 전용이 아니라 **거래처 양식에 종속되지 않는** 설계를 목표로 한다.
> 루미 프로덕션(`staff.html` / `functions/index.js`)은 이 문서로 인해 변경되지 않는다.

관련 문서: `BRIEF.md`(전체 지시서) · `DECISIONS.md` / `DECISIONS-2.md`(결정) · `AUDIT.md`(현행 조사) ·
`ISSUES.md`(미해결) · `HANDOFF.md`(기획 채팅 인계)

---

## 0. 한눈에

| # | 항목 | 결정 |
| --- | --- | --- |
| 1 | 부가세 | **세포함 단가 하나**로 간다. 과세/면세 구분 v1 제외. 공급가액·세액은 `movements`에 **저장만**(화면 미표시) |
| 2 | OCR 방식 | Vision+좌표 파서 vs 비전 LLM → **§3 비교**. 권고: **비전 LLM 1차 + 산식 검산**, 좌표 파서는 예비 |
| 3 | 품목 매칭 | **거래처 품목코드가 1순위 키**. `items.vendorCodes: [{vendorId, code, packSize, lastPrice}]` |
| 4 | 할인·배송비·반품 | 명세서 꼬리의 금액 행은 **품목별 금액 비율로 안분**해 단가에 반영 |
| 5 | 양식 비종속 | 머리글 자동 판별 + **열 동의어 사전** + 벤더 프로파일 캐시 |

**결론 한 줄**: 건당 비용은 어느 쪽을 골라도 월 수천 원 수준이라 **의사결정 요인이 아니다.**
정확도와 공수만 보면 되고, 그 기준에서는 비전 LLM이 앞선다. 단, **저장 전 사람이 보는 화면**은 어느 쪽이든 필수다.

---

## 1. 표본 — (주)플랜닥스 거래명세표 2장

설계의 근거는 첨부받은 실제 명세서 2장이다. 아래는 그 구조를 그대로 옮긴 것이다.

### 1-1. 문서 구조

```
[머리글]  거래명세표 / 발행일
          ┌ 공급받는자 ┐        ┌ 공급자 ┐
          등록번호 / 상호(법인명) / 사업장주소 / 전화번호 / 합계금액
[표]      월 | 일 | 품목·규격 | 단위 | 수량 | 단가 | 공급가액 | 세액
[꼬리]    *** 이 하 여 백 ***  (필러)
          소계 : 수량 / 공급가액 / 세액
          합계금액
```

- 공급자 등록번호(플랜닥스 `199-81-02516`)는 **사업자번호 = 거래처 식별키**로 쓰기 좋다.
  상호는 표기 흔들림(㈜/주식회사/(주))이 있지만 사업자번호는 흔들리지 않는다.

### 1-2. 표에서 반드시 다뤄야 하는 것들

| 현상 | 예 | 처리 |
| --- | --- | --- |
| **품목 셀이 2행** | 1행 `PRIME SYRINGE 일회용 주사기/5ml,23G×25mm/100개1박스`<br>2행 `[ 1000017590 ]` | 두 행을 한 품목으로 병합. 대괄호 안이 **거래처 품목코드** |
| **장식 접두어** | `⭐플닥추천⭐`, `🌸1개도 특가🌸` | 정규화 단계에서 제거(이름 유사도만 오염, 코드 매칭에는 영향 없음) |
| **규격이 이름에 붙어 있음** | `/5ml,23G×25mm/100개1박스` | `/` 기준 분리 시도 → `spec`, 그리고 `100개1박스`에서 **packSize** 추정 |
| **필러 행** | `*** 이 하 여 백 ***`, 빈 행 | 스킵 |
| **할인 행** | `주문할인` 수량 1 · 단가 `-2,200` · 공급가액 `-2,000` · 세액 `-200` (명세서 ①)<br>`-20,880` / `-18,982` / `-1,898` (명세서 ②) | **품목이 아니다.** 안분 대상(§5) |
| **소계 행** | ① 수량 16 / 공급가액 95,368 / 세액 9,532 → 합계 **104,900**<br>② 수량 33 / 공급가액 279,440 / 세액 27,940 → 합계 **307,380** | 품목 아님. **검산 기준값**(§7) |

### 1-3. ★ 산식 검증 — 인쇄된 단가는 이미 세포함이다

두 명세서의 개별 행을 검산하면 이렇게 나온다.

```
6,880 × 3 = 20,640   ≠ 공급가액 18,764
                     = 공급가액 18,764 + 세액 1,876   ✔
3,100 × 1 =  3,100   ≠ 공급가액  2,819
                     = 공급가액  2,819 + 세액   281   ✔
```

즉 **`단가 × 수량 = 공급가액 + 세액`** 이 성립한다.
회계 문서의 통상 규칙(`단가 × 수량 = 공급가액`)과 다르고, 이 명세서는 **소비자가(세포함)를 단가 칸에 인쇄**한다.

이게 왜 중요한가:

1. **"재고닥은 세포함 단가 하나로 간다"는 방침이 실제 서류와 정확히 일치한다.**
   화면에서 부가세를 다루지 않아도 사람이 종이에서 읽는 숫자와 앱의 숫자가 같다.
2. 파서는 이 관계를 **검산식으로 쓸 수 있다.** `|단가×수량 − (공급가액+세액)| ≤ 1` 이면 그 행은 OCR이 맞은 것이다.
3. **단, 이건 플랜닥스 양식의 성질이지 보편 규칙이 아니다.** 다른 거래처는 `단가×수량 = 공급가액`(세별도)일 수 있다.
   → 그래서 파서는 **두 가설을 다 세우고 소계로 판정**해야 한다(§4-5, §7).

---

## 2. 부가세 방침 (지시 ①)

### 2-1. 확정

- **화면·입력·재고 계산은 전부 「세포함 단가」 하나로만 한다.**
- **과세/면세 품목 구분은 v1에서 하지 않는다.** (의료 소모품은 면세·과세가 섞이는데, 이걸 품목마다 붙이면
  소규모 병원 사용자가 못 채운다. 세무 신고는 어차피 세무사가 세금계산서로 한다.)
- **명세서에서 읽은 공급가액·세액은 `movements`에 저장만 하고 화면에 안 띄운다.**
  나중에 매입 집계·세무 연동을 붙일 때 원천 데이터가 남아 있어야 하므로 버리지 않는다.

### 2-2. 저장 위치 — `movements`는 아직 없다

현재 루미 코드에는 `movements` 컬렉션이 **없다.** 입고 이력은 두 컬렉션으로 갈라져 있다
(`AUDIT-REPLY.md`에 기록한 그대로).

| 현행 | 쓰는 곳 | 필드 |
| --- | --- | --- |
| `receiveHistory` | 재고 탭 📥입고 (`staff.html:15671`) | `itemId, receiveDate, locations{}, totalQty, purchasePrice, unitPrice, vendorId, vendorName` |
| `receivingHistory` | 입고 탭 (`staff.html:16918`) | `itemId, batchNo, qty, expiryDate, location, price, memo` |

**재고닥에서는 이 둘을 `clinics/{clinicId}/movements` 하나로 통합한다.** 명세서 OCR은 그 통합 스키마에 쓴다.
(현행 두 컬렉션의 통합은 이 문서의 범위 밖 — Phase 1 이후 과제.)

### 2-3. `movements` 저장 필드 (세금 관련만)

```js
// clinics/{clinicId}/movements/{movementId}
{
  type: 'in',                 // in | out | adjust | return
  itemId: 'inv_abc123',
  qty: 3,
  unitPrice: 6880,            // ★ 세포함. 화면에 보이는 유일한 단가
  amount: 20640,              // = unitPrice * qty (안분 반영 후)

  // ── 명세서에서 읽은 원본. 저장만 하고 화면에 안 띄운다 ──
  tax: {
    source: 'ocr',            // ocr | manual | null
    supplyAmount: 18764,      // 공급가액
    vatAmount: 1876,          // 세액
    printedUnitPrice: 6880,   // 명세서에 인쇄된 단가 (안분 전)
    vatIncluded: true,        // 인쇄 단가가 세포함인지 (§1-3 판정 결과)
  },

  // ── 안분 내역 (§5) ──
  allocation: {
    discount: -424,           // 이 행에 안분된 할인 (원)
    shipping: 0,
    basis: 20640,             // 안분 기준이 된 세포함 금액
  },

  purchaseDocId: 'pdoc_...',  // 원본 명세서 (§6-3)
  vendorId: 'v_plandocs',
  createdAt, createdBy, ...
}
```

`tax` 하위로 묶어 두는 이유: **화면 코드가 실수로 읽지 않게** 하기 위해서다.
`unitPrice`만 보면 되는 UI가 `supplyAmount`를 집어 쓰는 사고를 구조로 막는다.

---

## 3. 방식 비교 — Vision + 좌표 파서 vs 비전 LLM (지시 ②)

### 3-0. 먼저: 이 비교의 한계를 명시한다

**아래 정확도 숫자는 실측이 아니다.** 나는 이 세션에서 GCP 자격증명이 없어 Vision API를 호출할 수 없고,
비전 LLM에 이미지를 넣어 돌려볼 수단도 없다. 숫자는 **첨부한 명세서 2장의 구조를 보고, 현행
`ocrDailySales` 파서가 같은 종류의 표에서 실제로 어떻게 동작하는지에 근거해 추론한 값**이다.
**돈을 쓰기 전에 §3-6의 실측 절차를 반드시 돌려야 한다.**

### 3-1. A안 — Google Cloud Vision + 좌표 파서 (현행 자산 재사용)

이미 만들어져 있는 것 (`functions/index.js`):

| 함수 | 줄 | 하는 일 |
| --- | --- | --- |
| `layoutWordRows()` | 787 | Vision 응답 → `{text, x, x1, y, h}` 단어 배열. **기울기 보정**(글상자 윗변 각도)까지 함 |
| `dsRowNumbers()` | 1670 | 한 행에서 숫자 토큰 추출 (`%` 제거, `1,234`·`1.234` 표기 흡수) |
| `dsAnchors()` | 1738 | 완전한 행들의 **열별 x 중앙값**으로 열 기준선 산출. 실패 시 우측정렬 등간격 |
| `dsAlign()` | 1702 | 토큰↔열 **DP 정렬**. 빈 칸은 0, 열 밖 토큰은 DROP |
| `dsVerify*()` | 1879~ | 표의 산식으로 검산 → `warnings` |
| `DS_*_COLS` | 1573~ | 열 정의만 갈아끼우면 다른 표가 됨 |

코드 주석이 설계 의도를 그대로 적어 놨다 — *"이 함수는 '자동 입력'이 아니라 '받아쓰기 + 검산'이다. 저장 전 사람이 본다."*
이 철학은 명세서에도 그대로 유효하다.

**명세서에 재사용할 때 새로 만들어야 하는 것**:
열 자동 판별(양식 비종속), 2행 셀 병합, 대괄호 코드 추출, 머리글 파싱, 할인 행 인식, 안분.

**약점**:
- 열 정의가 **고정 개수**여야 `dsAnchors(rowTokens, expected)`가 산다. 거래처마다 열 수가 다르면 `expected`를 먼저 알아내야 하고, 그게 §4의 머리글 판별이 필요한 이유다.
- **2행 셀 병합**이 좌표 로직만으로는 까다롭다. 품목명 행과 코드 행의 y 간격이 행간과 비슷하면 오병합·미병합이 난다.
- 사진이 기울거나 접히면 열 기준선이 무너진다. (기울기 보정은 있지만 곡면 왜곡은 못 잡는다.)
- **거래처가 늘 때마다 사람이 손을 대야 할 확률이 높다.** SaaS에서 이건 운영비다.

### 3-2. B안 — 비전 LLM에 이미지를 주고 JSON 스키마로 받기

한 번의 호출로 머리글·표·꼬리를 통째로 구조화한다.

```
[system] 너는 거래명세표를 읽어 JSON으로 만든다. 다음 스키마를 정확히 따른다. 추측하지 말고
         읽히지 않으면 null. 숫자는 콤마 없이 정수. 인쇄된 그대로만 쓴다.
[user]   <이미지>
[schema] { vendor:{bizNo,name,tel,addr}, issuedAt,
           lines:[{month,day,name,spec,vendorCode,unit,qty,unitPrice,supplyAmount,vatAmount,raw}],
           adjustments:[{kind:'discount'|'shipping'|'return'|'other', label, amount}],
           totals:{qty,supplyAmount,vatAmount,grandTotal} }
```

**강점**:
- **양식 비종속이 공짜로 온다.** 열 순서가 달라도, 열이 하나 더 있어도, 머리글 문구가 달라도 읽는다.
- **2행 셀 병합·장식문자 제거·대괄호 코드 추출이 자연스럽다.** A안에서 제일 손 많이 가는 부분이 사라진다.
- `주문할인` 같은 행을 `adjustments`로 **의미 분류**해서 준다. A안은 라벨 정규식으로 잡아야 한다.
- 초기 공수가 압도적으로 작다.

**약점**:
- **숫자 환각(hallucination).** 이게 유일하지만 치명적인 약점이다. 좌표 파서는 못 읽으면 빈칸이지만,
  LLM은 **그럴듯한 숫자를 만들어 낼 수 있다.** 재고·금액 데이터에서 이건 조용한 오염이다.
  → **§7 검산이 선택이 아니라 필수인 이유.**
- 응답이 비결정적이다. 같은 이미지에 두 번 다른 답이 나올 수 있다(temperature 0로 완화, 제거는 아님).
- 외부 벤더 의존 — 가격·모델 수명·지연시간이 우리 통제 밖.
- **이미지가 외부 API로 나간다.** 명세서에는 병원 상호·사업자번호·주소·전화번호가 찍혀 있다.
  민감도는 낮지만(진료정보 아님) SaaS 약관·개인정보 처리방침에 **처리위탁으로 명시**해야 한다.

### 3-3. 정확도 — 명세서 2장 기준 추론

두 장의 실제 난점별로, 각 방식이 어떻게 될지를 적는다. 판정은 추론이다(§3-0).

| 난점 | 이번 2장에 나오는 형태 | A안 (Vision+좌표) | B안 (비전 LLM) |
| --- | --- | --- | --- |
| 열 순서·개수 파악 | 8열 고정 | 머리글 판별 붙이면 ○ / 안 붙이면 ✕ | ○ (스키마가 흡수) |
| 2행 셀 병합 | 품목명 + `[ 1000017590 ]` | △ — 별도 로직 필요, 행간 오판 위험 | ○ |
| 장식 접두어 | `⭐플닥추천⭐`, `🌸1개도 특가🌸` | ○ (정규식) | ○ |
| 규격 분리 | `/5ml,23G×25mm/100개1박스` | △ (구분자 규칙 의존) | ○ |
| 숫자 정확도 | 콤마·음수·소수 | **◎ — 픽셀에서 직접 읽음. 환각 없음** | △ — **환각 가능** |
| 음수 행 인식 | `주문할인 -2,200` | ○ (`-?` 정규식 있음) | ○ (의미까지 분류) |
| 필러 행 스킵 | `*** 이 하 여 백 ***` | ○ | ○ |
| 소계 검산 | 16 / 95,368 / 9,532 | ◎ | ○ (숫자 자체가 환각이면 검산도 같이 틀림) |
| **처음 보는 거래처 양식** | (이번 2장에는 없음) | **✕~△ — 손봐야 할 확률 높음** | **◎** |

정리하면: **A안은 "읽은 숫자는 믿을 수 있는데 표 구조를 못 따라간다", B안은 "구조는 잘 따라가는데 숫자를 믿을 수 없다."**
그리고 두 약점은 성격이 다르다. **구조 실패는 화면에서 눈에 보이고(칸이 어긋남), 숫자 환각은 안 보인다.**

거친 기대치(같은 거래처, 깨끗한 사진, 필드 단위):

| | A안 | B안 |
| --- | --- | --- |
| 같은 양식 (프로파일 학습됨) | 96~99% | 95~98% |
| 처음 보는 양식 | 60~85% (열 판별 성공 여부에 좌우, 실패 시 급락) | 90~96% |
| 무보정 사진(기울기·그림자) | 낮음 | 중간 |

### 3-4. 공수

| 작업 | A안 | B안 |
| --- | --- | --- |
| 머리글 파싱(거래처·사업자번호·일자) | 1.0인일 | 0 (스키마 포함) |
| 표 영역 탐지 + 열 자동 판별 | **3.0인일** | 0 |
| 2행 셀 병합 + 코드 추출 | 1.5인일 | 0 |
| 행 조립·필러/소계 제외 | 1.0인일 | 0.3인일 |
| 프롬프트·스키마 설계 + 튜닝 | — | **2.0인일** |
| 검산 로직 (§7) | 1.0인일 | 1.0인일 (B는 더 중요) |
| 안분 (§5) | 0.5인일 | 0.5인일 |
| 품목 매칭 3단 (§6) | 2.0인일 | 2.0인일 |
| 확인·수정 화면 (§8) | 2.5인일 | 2.5인일 |
| 벤더 프로파일 캐시 | 1.0인일 | 0.5인일 |
| **합계** | **13.5인일** | **8.8인일** |
| **+ 거래처 추가 시 운영 공수** | 양식당 0.3~1.0인일 | 0 (프롬프트 예시 추가 정도) |

마지막 줄이 SaaS에서 결정적이다. **A안은 고객이 늘수록 개발자 시간이 든다.**

### 3-5. 건당 비용

**A안**: Vision `DOCUMENT_TEXT_DETECTION` 1장 = 1 unit.
개략 **$0.0015/장 ≈ 2원/장** (월 1,000 unit 무료 구간 있음).

**B안**: 명세서 한 장(장변 1,600px 기준) ≈ 이미지 1,500~2,500 입력토큰.
프롬프트+스키마 ≈ 800~1,200. 출력 JSON(품목 10~20행) ≈ 1,200~2,000 토큰.

```
건당 비용 ≈ (입력토큰 ≈ 3,500) × 입력단가 + (출력토큰 ≈ 1,600) × 출력단가
```

| 모델 급 | 대략 단가(입/출, $/1M) | 건당 |
| --- | --- | --- |
| 소형 비전 모델 | ~$1 / ~$5 | ≈ $0.011 ≈ **15원** |
| 중형 비전 모델 | ~$3 / ~$15 | ≈ $0.034 ≈ **47원** |

> ⚠️ **단가는 착수 시점에 실제 요금표로 다시 계산할 것.** 위 단가는 개략치이고, 나는 지금 확인할 수단이 없다.

**월 비용 (소규모 병원 = 명세서 월 30~60장 가정)**

| | A안 | B안(소형) | B안(중형) |
| --- | --- | --- | --- |
| 월 50장 | 100원 | 750원 | 2,350원 |
| 병원 100곳 × 월 50장 | 1만원 | 7.5만원 | 23.5만원 |

**결론: 건당 비용은 의사결정 요인이 아니다.** 병원 100곳을 받아도 월 최대 24만 원이고,
A안이 아낀 23만 원은 **A안 추가 공수 4.7인일 + 거래처마다 붙는 운영 공수**보다 훨씬 싸지 않다.

### 3-6. 권고 — B안 1차 + 산식 검산, A안은 예비

```
사진 → [전처리] → 비전 LLM → JSON
                                 ↓
                        [산식 검산 §7]  ── 통과 → 확인 화면(사람) → 저장
                                 ↓ 실패
                        [Vision 좌표 파서로 재시도]  또는  [전 행 사람 확인 모드]
```

이유:

1. **SaaS의 진짜 비용은 거래처 양식이지 API 요금이 아니다.** B안은 그 비용이 0에 가깝다.
2. **환각은 검산으로 상당 부분 막힌다.** 명세서에는 검산에 쓸 수 있는 산식이 세 겹으로 있다(§7).
   행 산식·소계·합계가 다 맞는데 숫자가 틀릴 확률은 매우 낮다.
3. **저장 전 사람이 보는 화면이 어차피 필요하다.** 이건 A/B 공통이고, 현행 `ocrDailySales`가
   이미 그 철학으로 만들어져 있다. 즉 최종 안전망은 방식 선택과 무관하게 존재한다.
4. A안 자산(`layoutWordRows`/`dsAnchors`/`dsAlign`)은 **버리는 게 아니라 폴백으로 남는다.**
   검산이 깨진 명세서에서 "그래도 픽셀에서 직접 읽은 숫자"를 대조군으로 쓸 수 있다.

**단, 착수 전에 다음 실측을 반드시 한다:**

- 골든셋 **20~30장** — 거래처 최소 5곳, 좋은 사진/기운 사진/그림자/접힘 섞어서.
- 각 장의 정답을 **사람이 손으로 입력**해 둔다(품목명·코드·수량·단가·공급가액·세액·소계).
- 두 방식을 같은 셋에 돌려 **필드 단위 정확도 / 행 누락률 / 행 환각률 / 검산 통과율 / 사람 수정 클릭 수**를 잰다.
- **`사람 수정 클릭 수`가 진짜 지표다.** 정확도 97%라도 어디가 틀렸는지 모르면 사람은 전 행을 다시 본다.

---

## 4. 양식 비종속 파이프라인 (지시 ⑤)

방식이 A든 B든 파이프라인 골격은 같다. 다른 건 3단계와 4단계를 누가 하느냐뿐이다.

```
① 전처리      촬영 이미지 → 리사이즈(장변 1600~2000px) / JPEG 0.85~0.92
              (현행 staff.html의 canvas 압축 방식과 동일)
② 텍스트화    A안: Vision DOCUMENT_TEXT_DETECTION → layoutWordRows()
              B안: 비전 LLM에 그대로 투입
③ 머리글 판별  거래처(사업자번호·상호), 발행일, 문서 종류(거래명세표/세금계산서/영수증)
④ 표 판별     헤더 행 탐지 → 열 매핑 → 데이터 행 범위 확정
⑤ 행 조립     2행 셀 병합 / 필러·소계 제외 / 품목 vs 조정행(할인·배송비·반품) 분류
⑥ 검산        §7
⑦ 안분        §5
⑧ 품목 매칭   §6
⑨ 확인 화면   사람이 보고 고친 뒤 저장  ← 생략 불가
```

### 4-1. ★ 열 동의어 사전 — 양식 비종속의 핵심

거래처마다 열 이름이 다르다. 그래서 **열을 이름이 아니라 「의미」로 잡는다.**

```js
const COLUMN_SYNONYMS = {
  date:         ['월', '일', '일자', '날짜', '거래일', '납품일'],
  name:         ['품목', '품명', '품목명', '상품명', '제품명', '규격', '품목/규격', '내역', '적요'],
  spec:         ['규격', '사양', 'SPEC', '표준'],
  vendorCode:   ['코드', '품목코드', '상품코드', '제품코드', 'CODE', '바코드'],
  unit:         ['단위', 'UNIT', '수량단위'],
  qty:          ['수량', '납품수량', '출고수량', '개수', 'QTY'],
  unitPrice:    ['단가', '판매단가', '납품단가', '단가(원)', '소비자가'],
  supplyAmount: ['공급가액', '공급가', '금액', '공급대가', '과세표준'],
  vatAmount:    ['세액', '부가세', '부가가치세', 'VAT', '세액(원)'],
  amount:       ['합계', '합계금액', '총액', '금액계'],
};
```

판별 규칙:

1. 헤더 행 후보 = 위쪽 30% 안에서 **동의어 사전에 3개 이상 걸리는 행**.
2. 걸린 토큰의 x 범위로 **열 경계**를 만든다. (A안은 여기서 `dsAnchors()`에 `expected`를 넘긴다.)
3. `name`과 `spec`이 같은 열에 걸리면(`품목/규격`) 한 열로 보고 나중에 문자열 분리.
4. **헤더를 못 찾으면** → 데이터 행의 **자료형 패턴**으로 추정:
   맨 왼쪽 정수 2개(월·일) → 긴 한글/영문 열(품목) → 짧은 단위 열 → 우측 정렬 숫자 3~4열(수량·단가·공급가액·세액).
5. 그래도 실패하면 **사람에게 열을 지정하게 한다.** 그 지정을 벤더 프로파일에 저장한다(§4-3).

### 4-2. 세포함/세별도 판정 (§1-3의 일반화)

명세서마다 다르므로 **문서 단위로 판정**한다.

```js
// 각 행에서 두 가설의 잔차를 재고, 문서 전체에서 이긴 쪽을 채택
hypoIncluded = Σ | qty*unitPrice − (supply + vat) |
hypoExcluded = Σ | qty*unitPrice −  supply        |
vatIncluded  = hypoIncluded < hypoExcluded
```

- 플랜닥스 2장 → `vatIncluded = true` (§1-3).
- 공급가액·세액 열이 아예 없는 명세서 → `vatIncluded`를 판정할 수 없다.
  → **인쇄된 단가를 그대로 세포함으로 받는다.** 우리 방침이 세포함 하나이므로 그게 안전한 기본값이다.
- 판정 결과는 `movements.tax.vatIncluded`에 남긴다.

### 4-3. 벤더 프로파일 캐시 (학습)

한 번 성공한 양식은 다시 고생하지 않는다.

```js
// clinics/{clinicId}/vendorProfiles/{vendorId}   (또는 사업자번호 기준 전역 공유 검토)
{
  bizNo: '199-81-02516',
  vendorName: '(주)플랜닥스',
  docKind: 'invoice',
  columns: ['month','day','name','unit','qty','unitPrice','supplyAmount','vatAmount'],
  vendorCodePattern: '\\[\\s*(\\d{6,})\\s*\\]',   // 대괄호 안 숫자
  itemCellRows: 2,                                 // 품목 셀이 2행
  vatIncluded: true,
  decorPrefixes: ['⭐플닥추천⭐','🌸1개도 특가🌸'],
  confirmedCount: 12,     // 사람이 수정 없이 승인한 횟수 — 신뢰도
  updatedAt,
}
```

- B안에서는 이 프로파일을 **프롬프트 힌트**로 넣는다("이 거래처는 8열이고 품목 셀이 2행이다").
  정확도가 오르고 출력 토큰도 준다.
- A안에서는 열 판별을 통째로 건너뛴다.
- **전역 공유 여부는 미결(§10).** 사업자번호 기준으로 병원 간에 공유하면 새 고객이 첫날부터 잘 읽히지만,
  한 병원의 잘못된 확정이 다른 병원에 번진다.

---

## 5. 할인·배송비·반품 안분 (지시 ④)

### 5-1. 규칙

명세서 꼬리의 금액 행(`주문할인`, `배송비`, `반품`)은 **품목이 아니다.**
**각 품목의 세포함 금액 비율로 안분해 단가에 반영한다.**

이유: 재고닥의 단가는 **"이 물건이 실제로 얼마에 들어왔나"** 여야 한다.
할인을 별도 행으로 남기면 품목 단가가 실제보다 비싸게 잡히고, 발주 예측·소진 단가가 전부 틀어진다.

### 5-2. 알고리즘

```js
function allocate(lines, adjustments) {
  // lines[i].gross = 세포함 금액 (= printedUnitPrice * qty, 또는 supply + vat)
  const basis = lines.reduce((s, l) => s + l.gross, 0);
  if (basis <= 0) return lines.map(l => ({...l, alloc: 0}));

  for (const adj of adjustments) {          // adj.amount: 할인·반품은 음수, 배송비는 양수
    let acc = 0;
    lines.forEach((l, i) => {
      let share;
      if (i < lines.length - 1) {
        share = Math.round(adj.amount * l.gross / basis);
        acc += share;
      } else {
        share = adj.amount - acc;           // ★ 잔차는 마지막 행에 몰아 준다
      }
      l.alloc = (l.alloc || 0) + share;
    });
  }

  return lines.map(l => {
    const net = l.gross + (l.alloc || 0);
    return {
      ...l,
      amount: net,
      unitPrice: Math.round(net / l.qty),   // ★ 저장되는 단가 = 안분 반영 후
      allocation: {discount: l.alloc, basis: l.gross},
    };
  });
}
```

**잔차 처리가 중요하다.** 비율 배분은 원 단위 반올림 때문에 합이 안 맞는다.
마지막 행에 잔차를 몰아 주면 **Σ안분 = 조정액**이 정확히 성립하고, 소계 검산(§7)이 통과한다.

### 5-3. 실제 숫자로 계산해 본다 — 명세서 ①

명세서 ①: 합계 **104,900**, 주문할인 **-2,200**(세포함).
할인은 합계에 이미 반영돼 있으므로 **할인 전 품목 세포함 총액 = 104,900 + 2,200 = 107,100**.

| 행 | 세포함 금액(gross) | 안분 계산 | 안분액 | 반영 후 금액 | 반영 후 단가 |
| --- | ---: | --- | ---: | ---: | ---: |
| `6,880 × 3` | 20,640 | −2,200 × 20,640 / 107,100 | **−424** | 20,216 | **6,739** |
| `3,100 × 1` | 3,100 | −2,200 × 3,100 / 107,100 | **−64** | 3,036 | **3,036** |
| (나머지 행) | 83,360 | 나머지 | −1,712 | 81,648 | … |
| **계** | **107,100** | | **−2,200** | **104,900** ✔ | |

> 나머지 행은 이 문서에 옮겨 적지 않았다(표본 확인용 두 줄만 발췌). 잔차 −1,712는 총액 정합을 보인 것이다.

즉 `6,880`짜리로 보이던 물건이 **실제로는 6,739원에 들어왔다.** 이 차이가 발주 예측과 원가에 그대로 들어간다.

### 5-4. 반품(return)은 다르게 본다 — 검토 필요

할인·배송비는 **같은 거래의 금액 조정**이라 안분이 맞다.
그런데 **반품은 특정 품목의 수량 반환**인 경우가 많다. 이때 안분하면 엉뚱한 품목의 단가가 오염된다.

```
반품 행에 품목이 특정되는가?
 ├ YES → 안분하지 않는다. 그 품목의 movements 에 type:'return', qty 음수로 별도 기록.
 └ NO  → (금액만 있는 총액 반품) 할인과 동일하게 안분.
```

`adjustments[].kind === 'return'` 이면 파서가 **품목 특정 여부를 같이 넘겨야 한다**(`targetLineIndex`).
확인 화면에서 사람이 바꿀 수 있게 한다.

---

## 6. 품목 매칭 3단 (지시 ③)

### 6-1. `items.vendorCodes` — 지시받은 구조

```js
// clinics/{clinicId}/items/{itemId}
{
  name: '일회용 주사기 5ml',
  unit: '개',
  packSize: 100,                 // 우리 기본 입수 (없으면 1)
  ...
  vendorCodes: [
    {
      vendorId:  'v_plandocs',
      code:      '1000017590',   // ★ 매칭 키
      vendorName:'PRIME SYRINGE 일회용 주사기/5ml,23G×25mm/100개1박스',  // 명세서 표기 원문
      packSize:  100,            // 이 거래처가 1줄로 파는 단위 (박스=100개)
      lastPrice: 6739,           // 마지막 입고 단가 (세포함·안분 반영 후)
      lastAt:    '2026-08-21',
      confirmedBy:'김OO',        // 사람이 확정한 매핑임을 남긴다
    },
    { vendorId: 'v_other', code: 'SY-5ML-23G', packSize: 1, lastPrice: 78, ... },
  ],
}
```

- **품목 하나에 거래처별 코드가 여럿** 붙는다. 같은 주사기를 두 곳에서 사면 둘 다 등록된다.
- `packSize`가 있어야 **낱개 환산**이 된다. 명세서 `수량 3`이 박스 3개면 재고는 **300개** 증가다.
  이걸 안 하면 재고가 100배 틀린다. **v1에서 반드시 넣어야 하는 필드다.**
- `lastPrice`는 **안분 반영 후 세포함 단가**를 넣는다(§5). 가격 변동 알림의 기준값이 된다.

**인덱스**: `vendorCodes`는 배열이라 Firestore `array-contains`로는 객체 부분일치 검색이 안 된다.
→ 조회용으로 **평탄화 키**를 같이 둔다.

```js
vendorCodeKeys: ['v_plandocs::1000017590', 'v_other::SY-5ML-23G']   // array-contains 로 1회 조회
```

### 6-2. 3단 매칭

```
[1단] 거래처 품목코드   ── 정확일치 ──→ 신뢰도 100%  자동 매칭
      key = `${vendorId}::${normalizeCode(code)}`
      normalizeCode: 공백·대괄호 제거, 대문자화, 앞 0 유지
         '[ 1000017590 ]' → '1000017590'

[2단] 입고 이력          ── 같은 거래처의 과거 명세서 원문과 일치 ──→ 신뢰도 90%  자동 매칭(표시)
      movements 에 남은 rawName 을 정규화해 비교.
      코드가 안 찍히는 거래처를 위한 경로다.

[3단] 이름 유사도        ── 후보 상위 3개 제시 ──→ 신뢰도 <90%  사람이 고른다
      정규화: 장식문자 제거(⭐🌸 등) → 규격 분리 → 단위 통일(㎖/ml, ㎝/cm)
             → 브랜드 토큰 분리 → 자모 분해 후 유사도
      점수 = 0.5*토큰겹침 + 0.3*문자열유사도 + 0.2*(단위·규격 일치)
```

**사람이 한 번 고르면 그 선택을 `vendorCodes`에 자동 등록한다.**
이게 이 설계의 핵심이다 — **3단 → 1단으로 승격되며, 두 번째 명세서부터는 자동이다.**

### 6-3. 미매칭 처리

```
후보 없음 → [신규 품목으로 등록] / [기존 품목에 연결] / [이번엔 건너뜀]
```

- **[건너뜀]을 반드시 둔다.** 명세서에 재고 관리 대상이 아닌 것(사은품, 판촉물)이 섞인다.
  건너뛴 코드는 `vendorCodeIgnored`에 남겨 다음부터 안 묻는다.
- **[신규 등록]은 기본값을 명세서에서 채운다** — 이름·규격·단위·packSize·단가·거래처·코드.
  사용자는 카테고리와 보관 장소만 고르면 된다.

---

## 7. 검산 — OCR은 반드시 틀린다는 전제

현행 `ocrDailySales`의 철학을 그대로 가져온다. **명세서는 검산식이 세 겹으로 있어서 유리하다.**

| 층 | 식 | 깨지면 |
| --- | --- | --- |
| **행** | `qty × printedUnitPrice == supply + vat` (세포함) 또는 `== supply` (세별도) | 그 행만 빨갛게 |
| **열** | `Σ qty == 소계 수량`, `Σ supply == 소계 공급가액`, `Σ vat == 소계 세액` | 행 누락/환각 의심 |
| **문서** | `소계 공급가액 + 소계 세액 == 합계금액`, `Σ gross + Σ adjustments == 합계금액` | 조정행 누락 의심 |

허용 오차: 원 단위 반올림 때문에 **행별 ±1원, 문서 합계 ±(행 수)원**까지 통과.

**B안(비전 LLM)에서 이 검산이 특히 중요하다.**
- 세 층이 모두 통과 → 환각 확률 매우 낮음 → 자동 채움 + 사람은 훑어보기만.
- 한 층이라도 깨짐 → **전 행 사람 확인 모드**로 전환하고, A안 폴백을 병행 실행해 대조군을 보여준다.
- **행 개수 검산**을 따로 둔다: `소계 수량 == Σ qty`가 맞아도 행이 통째로 빠질 수 있으므로,
  이미지에서 센 데이터 행 수와 JSON 행 수를 비교한다(A안 폴백이 이걸 잘 한다).

---

## 8. 확인·수정 화면 (저장 전 사람이 본다)

```
┌ 명세서 사진 (핀치 줌) ┬ 파싱 결과 표 ──────────────────────────┐
│                        │ ✅ 일회용 주사기 5ml   3박스 → 300개    │
│   (원본 이미지)        │    6,739원/개  [플랜닥스 1000017590]   │
│                        │ ⚠️ 알콜솜            1  → ?            │
│                        │    후보: ①알콜스왑 ②알콜솜(대) ③신규  │
│                        │ ⛔ 3행: 단가×수량 ≠ 공급가액+세액       │
│                        │────────────────────────────────────────│
│                        │ 주문할인 −2,200 → 전 품목에 안분됨      │
│                        │ 소계 검산 ✔  합계 104,900 ✔            │
└────────────────────────┴────────────────────────────────────────┘
             [모두 확인했습니다 — 입고 처리]
```

원칙:

- **원본 사진을 옆에 붙여 둔다.** 사람이 대조할 수 없으면 확인 화면이 아니라 승인 버튼일 뿐이다.
- **어디가 미심쩍은지 색으로 구분한다.** 정확도보다 "틀린 곳을 찾는 시간"이 실제 사용성이다.
- **보관 장소**는 명세서에 없으므로 반드시 사람이 고른다. 품목별 마지막 장소를 기본값으로 제시.
- **유효기간**도 명세서에 대개 없다. 필요한 품목만 묻는다.
- 저장은 **한 번에 트랜잭션**으로. 부분 저장되면 재고가 이상해진다.

---

## 9. 공수 (권고안 = B안 1차 + A안 폴백 기준)

| 단계 | 인일 |
| --- | ---: |
| 골든셋 구축 + 실측 비교(§3-6) | 2.0 |
| 비전 LLM 프롬프트·스키마·튜닝 | 2.0 |
| 머리글/열 동의어 사전 + 벤더 프로파일 | 1.5 |
| 검산 3층 (§7) | 1.0 |
| 안분 (§5) | 0.5 |
| 품목 매칭 3단 + `vendorCodes` 스키마 (§6) | 2.0 |
| 확인·수정 화면 (§8) | 2.5 |
| 저장 트랜잭션 + `movements` 연동 (§2) | 1.0 |
| A안 폴백 결선 (기존 `dsAlign` 재사용) | 1.5 |
| **합계** | **14.0인일** |

전제: `movements` 통합 스키마가 이미 있다고 본다. 없으면 그 작업(별건)이 선행돼야 한다.

---

## 10. 미결 — 착수 전에 답이 필요한 것

| # | 질문 | 왜 지금 정해야 하나 |
| --- | --- | --- |
| 1 | 벤더 프로파일을 **병원 간에 공유**할까? | 공유하면 신규 고객 첫날부터 잘 읽힌다. 대신 한 병원의 오확정이 번진다. 스키마 위치가 달라진다 |
| 2 | 명세서 **원본 이미지를 보관**할까? 보관하면 기간은? | Storage 비용·개인정보 보관기간. 확인 화면에서 대조하려면 최소 며칠은 필요 |
| 3 | 비전 LLM **벤더 선정** | 요금·지연·약관(처리위탁). 처리방침 문구가 달라진다 |
| 4 | `packSize`가 틀렸을 때 **재고 100배 오류** 안전장치 | 예: 직전 재고 대비 10배 이상 증가하면 경고. 규칙을 정해야 한다 |
| 5 | 반품 행의 **품목 특정 규칙** (§5-4) | 안분/별도기록 분기 기준 |
| 6 | `receiveHistory` / `receivingHistory` → `movements` **통합 시점** | 이 기능이 그 위에 올라간다. 선행 과제 |
| 7 | 세금계산서·영수증도 같은 파이프라인으로 받을까? | `docKind` 분기 설계가 달라진다 |

---

## 11. 참조

| 대상 | 위치 |
| --- | --- |
| Vision 호출 (영수증) | `functions/index.js:1350` `exports.ocrReceipt` |
| Vision 호출 (매출 결산표) | `functions/index.js:2161` `exports.ocrDailySales` |
| 단어+좌표 재구성 (기울기 보정) | `functions/index.js:787` `layoutWordRows()` |
| 줄 단위 텍스트 | `functions/index.js:858` `layoutLines()` |
| 숫자 토큰 추출 | `functions/index.js:1670` `dsRowNumbers()` |
| 토큰↔열 DP 정렬 | `functions/index.js:1702` `dsAlign()` |
| 열 기준선 산출 | `functions/index.js:1738` `dsAnchors()` |
| 열 정의 (갈아끼우는 지점) | `functions/index.js:1573` `DS_PAY_COLS` / `:1607` `DS_SALES_COLS` / `:1623` `DS_PROC_COLS` |
| 검산 → warnings | `functions/index.js:1879` `dsVerifyPayment()` 외 |
| 현행 입고 ① (재고 탭 📥) | `staff.html:15671` → `receiveHistory` |
| 현행 입고 ② (입고 탭) | `staff.html:16918` → `receivingHistory` |
| 가격 이력 | `staff.html:15653` `priceHistory` (최근 5개) |
| 거래처 | `staff.html:14374` `vendors` 컬렉션 (실시간 스냅샷) |
