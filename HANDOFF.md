# HANDOFF.md — 상용화 기획팀 전달 문서

- 작성: 2026-08-22 (개발 세션 → 기획 세션)
- 기준 커밋: `bbbfb57` (main) — 2026-08-22 갱신
- 원 조사 기준: `c656612` (`AUDIT.md`)

**이 문서를 읽는 사람:** `lumi-admin` 을 SaaS 로 상용화하는 기획을 진행 중인 쪽.
개발 세션에서 코드를 실측·조사한 결과 중 **기획 전제를 바꾸는 것만** 추렸습니다.
구현 세부는 `AUDIT.md` / `ISSUES.md` 를 보시면 됩니다.

---

## 0. 한눈에 — 기획이 반드시 알아야 할 6가지

| # | 사실 | 기획에 미치는 영향 |
|---|---|---|
| 1 | **루미는 하루 38커밋 속도로 개발 중** (`staff.html` 만 27회, 컬렉션 5개 신규) | SaaS 개조는 **움직이는 표적**을 쫓는 일. 장기 분기 브랜치는 성립하지 않음 |
| 2 | **`BRIEF.md` 의 3개 모듈 밖에 큰 덩어리가 있음** — 매출·비용·세무 약 5,100줄 | 판매 모듈 구성을 다시 봐야 함 |
| 3 | **`DECISIONS-2.md` 결정 A 의 근거가 실측으로 무너짐** | 신규 병원 입고 경로를 반대로 뒤집어야 함 |
| 4 | **CRM 격리는 `staff.html` 의 약 30% 를 건드리는 대수술** (약 5,050 / 18,498 줄) | 기획 확정 전에 착수하면 되돌리는 비용이 큼 |
| 5 | **Firestore Rules 를 공개 홈페이지와 공유 중** | 멀티테넌시 Rules 재작성은 2단계로만 가능 |
| 6 | 🆕 **관리자앱(`index.html`) 폐기 확정** — 통합앱 단일 기준 | 범위가 **9,555 줄 줄어듦**. 대신 비용·세무·손익의 거취 결정 필요 |

---

## 1. 프로덕션에 이미 반영된 것 (오류 수정 2건)

기획과 무관하게 **재고 숫자가 틀리고 있어서** 먼저 고쳤습니다. 리팩터링 없이 오류만 수정.

| PR | 내용 |
|---|---|
| **#142** | 입고 탭이 `currentStock` 을 갱신하지 않던 오류. 입고해도 재고 목록 수량이 안 올라갔음 |
| **#146** | 입고 탭이 `totalStock` 만 읽어 **207 품목 중 178 품목(86%)** 의 현재고가 0 으로 표시되던 오류 |
| **#177** | 재고 목록 다운로드 버튼 먹통. `INV_TYPES` 가 `inventory.js`(관리자앱 전용)에만 있어 `staff.html` 에서 `ReferenceError`. `try/catch` 밖에서 터져 CSV 폴백까지 못 감 |

세 건 모두 각각 1~2 줄. 그 외 루미 운영 코드는 건드리지 않았습니다.

> 세 건 다 **두 앱이 재고 로직을 각자 갖고 있어서** 생긴 같은 뿌리의 버그입니다. §2-8 참조.

---

## 2. 실태 조사로 밝혀진 것 — 기획 전제를 바꾸는 사실들

### 2-1. 재고 데이터 오염은 없었다. 실사가 방어막이었다

프로덕션 전수 조사(207 품목) 결과 **`currentStock` 불일치 0 건.**

원인: **월 2회 실사가 `currentStock = Σlocations` 로 덮어씁니다.** 버그로 어긋나도 다음 실사에서
리셋되므로 손상 창이 최대 2 주였습니다.

> **기획 시사점:** "정기 실사"가 데이터 정합성의 실질적 안전장치입니다. SaaS 에서 실사 주기를
> 강제하거나 최소한 권장하는 온보딩이 필요합니다. 실사를 안 하는 병원은 재고 숫자가
> 서서히 어긋납니다.

### 2-2. 입고 경로가 두 개고, 하나는 사실상 안 쓰인다 → 결정 A 뒤집기

| | 재고 탭 `📥입고` | 입고 탭 |
|---|---|---|
| 실사용 (207 품목 중) | 다수 | **1 품목** |
| 유통기한 | ✅ | ✅ |
| 단가·구매가·가격이력 갱신 | ✅ | ❌ |
| 발주 예측 데이터 소스 | ✅ | ❌ |
| 배치(batch) 관리 | ❌ | ⚠️ **쓰기만 하고 읽는 코드가 없음 — 죽은 데이터** |
| 원자성(트랜잭션) | ❌ | ✅ |

`DECISIONS-2.md` 결정 A 는 *"배치·유통기한·트랜잭션을 갖춘 입고 탭이 우수하니 신규 병원엔
그것만 노출"* 이었는데, **근거 3 개 중 2 개가 사실이 아니었습니다.**

> **권고:** 신규 병원에는 **재고 탭 `📥입고` 경로**만 노출. 루미는 둘 다 유지.
> 분기는 `settings/org.receivingMode`. **아직 원장 승인 전입니다.**

### 2-3. 신규 병원의 첫 동작은 "입고"가 아니라 "이미 있는 재고 등록"

원장 확인 사항입니다. 재고앱을 도입하고 나서 입고가 시작되는 게 아니라, **도입 시점에 이미
재고가 있습니다.** 따라서 온보딩 첫 화면은 **초기 실사**여야 합니다.

> **미결:** 신규 병원 기본 진입 모드는 **Phase 5(온보딩) 설계 시 결정**하기로 보류.

### 2-4. 인센티브 — 실제로 동작하는 건 5 종, 죽은 게 2 종

`BRIEF.md` 는 인센티브를 3 종으로 줄이면 기존 5 종 중 3 종이 `manual` 로 퇴행한다고 봤는데,
**`base` enum 을 3 개(`ownRevenue` / `totalRevenue` / `totalMinusPersonal`)로 두면 5 종 전부
살아납니다. 퇴행 0.**

그리고 `incentiveItems` 의 `salesPercent`·`japanSales` 유형은 **UI 에서 만들 수는 있지만
급여 집계에서 한 번도 합산되지 않습니다.** 원장이 그 유형으로 항목을 만들면 화면엔 보이는데
급여엔 0 원으로 반영됩니다. 기능이 존재한 적이 없으므로 "빼는 것"이 아니라 "정리"입니다.

### 2-5. 직원 문서 ID 가 "이름"이다

`employees/{직원이름}` 구조이고, 이 ID 가 근태·OT·식대·연차·인센티브·급여명세서
**6 개 컬렉션의 외래키**로 박혀 있습니다.

- **동명이인 등록 불가** (앱이 막습니다)
- **개명 시 이력 단절**

원장 승인으로 `employees/{autoId}` + `authUid` 필드로 전환하기로 결정됐습니다(Phase 1, +2~3 인일).
Auth uid 를 문서 ID 로 쓰지 않는 이유는 **Auth 계정 없는 직원**(입사 예정자, 퇴사자 이력)을
등록할 수 없기 때문입니다.

### 2-6. Firestore Rules 를 공개 홈페이지와 공유하고 있다

`firestore.rules` 의 최상위 경로 중 **16 개는 이 레포에서 사용 0 회**입니다.
`lumiclinic.co.kr` 공개 홈페이지가 같은 Firebase 프로젝트·같은 Rules 파일을 씁니다.
그중 다수가 `allow read: if true`(비로그인 공개), `reservations` 는 `allow create: if true`.

> **시사점:** 멀티테넌시 Rules 는 **한 번에 갈아엎을 수 없습니다.**
> "새 `clinics/**` 블록 추가 → 이관 완료 후 기존 최상위 블록 단계적 제거" 2 단계 필수.

### 2-7. 매출 파서가 루미가 쓰는 EMR 포맷에 종속

`parseRevenueFile` 은 특정 EMR 의 엑셀 포맷(`오더별환자` 시트, 고정 컬럼명)에 맞춰져 있고,
Google Drive 폴더 ID 가 하드코딩되어 있습니다. 병원마다 EMR 이 다르면 그대로 안 돕니다.

원장 결정으로 **매출·비용·세무 전체를 `FINANCE_ENABLED` 플래그로 루미 전용 격리**하고
SaaS 에서는 제외하기로 했습니다. 대신 인센티브 계산에 필요한 월 매출은 **수동 입력 폼**으로
받습니다 — 오히려 EMR 종속이 없어져 어느 병원에나 붙습니다.

---

## 2-8. 🆕 관리자앱 폐기 확정 — 통합앱 단일 기준 (2026-08-22)

**원장 방침:** *"개발은 통합앱으로 계속 업데이트하고 있어서 앞으로 통합앱을 기준으로
업데이트하면 된다. 추후에는 관리자앱은 안 쓸 예정."*

### 폐기 대상 규모

| 파일 | 줄 |
|---|---|
| `index.html` | 1,068 |
| `expense.js` | 1,824 |
| `hr-attendance.js` | 1,861 |
| `expense-upload-parser.html` | 1,518 |
| `inventory.js` | 709 |
| `card-statements.js` | 626 |
| `firebase-config.js` | 590 |
| `revenue.js` | 584 |
| `finance.js` | 296 |
| `expense-categories.js` | 280 |
| `product-rating.js` | 199 |
| **합계** | **9,555 줄 (11 개 파일)** |

`firebase-config.js` 도 관리자앱 전용입니다 — 통합앱은 설정을 인라인으로 갖고 있습니다.

### 이 결정이 해소하는 것

**오늘 발생한 재고 버그 3 건이 전부 "두 앱이 같은 로직을 각자 갖고 있어서" 생겼습니다.**

| PR | 버그 |
|---|---|
| #142 | 입고 시 `currentStock` 미갱신 |
| #146 | `totalStock` 만 읽어 현재고 0 표시 |
| #177 | `INV_TYPES` 가 `inventory.js` 에만 있어 다운로드 버튼 먹통 |

관리자앱을 폐기하면 **이 버그 클래스 자체가 사라집니다.** 하드코딩 중복도 크게 줄어듭니다
(`roleLabels` 8 곳 중 5 곳, `INV_CATEGORIES` 일부가 폐기 대상 파일에 있음).

### ⚠️ 그런데 갈 곳이 없어지는 기능이 있습니다

컬렉션별로 어느 앱이 쓰는지 전수 조사한 결과입니다.

**통합앱에 이미 있음 — 폐기해도 안전**

`revenue` · `salesDetail` · `payroll` · `productRatings` · `inventory` · `employees`
그리고 근태·연차·식대·OT·인센티브·급여명세서.

> 통합앱이 이미 매출 Cloud Function(`listRevenueFiles`/`parseRevenueFile`)을 **직접 호출**하고
> 있습니다. 관리자앱이 아니라 통합앱이 매출 파싱의 주체입니다.
> 직원관리·급여·인센티브는 코드 주석에 *"admin 웹 직원관리 이관"*, *"통합앱 이관"* 으로
> 이미 옮겨진 것이 명시돼 있습니다.

**⚠️ 관리자앱 전용 — 폐기하면 기능이 사라짐 (8 개 컬렉션)**

| 컬렉션 | 기능 |
|---|---|
| `fixedExpenses` · `variableExpenses` | 고정비 · 변동비 |
| `cards` · `categoryRules` | 카드 명세서 · 비용 자동분류 |
| `incomeTaxes` · `vatTaxes` · `withholdingTaxes` | 소득세 · 부가세 · 원천세 |
| `config` | 비용 카테고리 설정 |
| `recipes` | 레시피(시술 원가) |

여기에 손익(P&L) 화면(`finance.js`)이 더해집니다.

**SaaS 관점에서는 문제없습니다** — `DECISIONS.md` 질문 1 에서 이미 매출·비용·세무를
판매 대상에서 제외하기로 했습니다.

**문제는 루미 자신입니다.** 관리자앱을 안 쓰면 **루미도 비용·세무·손익을 못 씁니다.**

> **미결:** 이 기능들을 (a) 통합앱으로 이관할지 (b) 버릴지 (c) 관리자앱만 남겨둘지.
> 세무·비용은 세무사 영역이라 실사용 빈도를 원장님이 판단해야 합니다.

### 계획에 미치는 영향

| 항목 | 변경 |
|---|---|
| `FINANCE_ENABLED` 격리 (Phase 0.5) | **거의 불필요.** 격리 대상이 폐기될 앱에 있음. 단 통합앱에도 매출 기능이 있어 그 부분만 남음 |
| Phase 1 이관 대상 | 관리자앱 전용 8 개 컬렉션 제외 → 축소 |
| Phase 3 하드코딩 제거 | 중복 수정 지점 감소 (`hr-attendance.js` 5 곳 등이 폐기 대상) |
| `AUDIT.md` §7 "재고 구현 두 벌" | **자연 해소** |
| 공수 | 감소 예상. 정확한 재추정은 위 미결 (a)/(b)/(c) 결정 후 |

---

## 3. 이번 세션에서 확정된 제품 결정

### 3-1. 타겟 — 직원 10인 전후 소규모 병원

기능을 넣을지 판단하는 **기준**으로 명문화했습니다.
**"자산 전체를 잡아야 하는" 기능은 넣지 않습니다.** 10 인 규모에서 관리 비용이 편익을
넘는 순간 그 기능은 안 쓰이게 됩니다.

### 3-2. 「개인 사무용품」은 수량 관리 대상이 아니다

무전기·컴퓨터·책상·받침대 같은 가끔 사는 개인 지급 물품.

| 항목 | 결정 |
|---|---|
| 수량 실사 / 개인별 배정 / 여분 관리 | **하지 않음** |
| 관리하는 것 | **구매처(링크) 하나뿐** |

실제 불편은 *"오랜만에 주문할 때 어디서 사야 하는지 한참 찾는 것"* 하나뿐입니다.

**그리고 이건 새로 만들 필요가 없습니다.** 기존 「🛍️ 비품 구입」 카탈로그가 정확히 그
기능입니다(내장 91 항목, 그중 85 개에 이미 구매 링크 존재). 분류 옵션 하나 추가(2 줄)면
끝이고, 그것도 Phase 3 에서 설정값으로 빼면서 같이 하면 됩니다.

### 3-3. 상담톡 웹훅 — 배포 해제

루미는 카카오·네이버 채널을 자체 웹훅에 연결한 적이 없어 정상 트래픽 경로가 없습니다.
HMAC 서명 검증을 구현하는 대신 **배포를 해제**하고, 코드와 `chatThreads` 데이터는 보존합니다.
재개발 시점에 검증과 함께 재배포.

---

## 4. ⚠️ 가장 중요한 제약 — 움직이는 표적

**조사 시작(`c656612`)부터 지금(`bbbfb57`)까지 하루 동안:**

| | |
|---|---|
| main 커밋 | **49 건** |
| `staff.html` 줄수 | 16,039 → **18,498** (**+2,459**) |
| 신규 Firestore 컬렉션 | **5 개** — `closingItems` `closingStatus` `fcmTokens` `activities` `users` |
| 신규 Cloud Function | **2 개** — `closingNightlyCheck` `onClosingDelegated` |

마감(closing) 기능과 FCM 푸시가 하루 만에 통째로 들어왔습니다.

### 이게 뜻하는 것

1. **`AUDIT.md` 의 줄 번호는 착수 시점에 다시 잡아야 합니다.** 조사 결론(구조·의존성)은
   전부 유효함을 재확인했지만, 위치는 매일 바뀝니다.
2. **장기 분기 브랜치는 현실적으로 불가능합니다.** 18,500 줄 단일 파일에서 5,000 줄을
   재배치하는 브랜치를 이 속도의 main 과 병행하면 며칠 안에 병합이 불가능해집니다.
3. **범위가 계속 늘어납니다.** 이관 대상 컬렉션이 하루에 5 개 늘었습니다. Phase 1
   공수 추정도 그만큼 흔들립니다.

### 루미 비접촉 제약과의 충돌

원장 방침: *"상용화 작업이므로 루미 것은 오류가 아니면 건드리지 말자."*

그런데 **멀티테넌시 개조는 정의상 루미를 건드리는 일입니다.** Phase 1 이 루미 데이터를
`clinics/lumi/` 하위로 옮기는 것이고, Phase 2 가 루미 계정에 Custom Claims 를 발급하는 것입니다.

분리 방식은 세 가지뿐이고 각각 대가가 다릅니다.

| | 방식 | 대가 |
|---|---|---|
| A | 장기 분기 브랜치 | `staff.html` 병합 지옥. 위 속도에서는 성립 안 함 |
| B | main 에 머지하되 플래그 기본값을 `true` 로 두어 루미 무동작 | 루미 코드에는 들어감. 회귀 위험 낮지만 0 은 아님 |
| C | 별도 레포로 포크 | 완전 분리. 대신 루미 버그픽스가 SaaS 로 안 넘어옴 (영구 이중 유지보수) |

**개발 측 판단은 B 입니다.** A 는 파일 구조상 성립하지 않고, C 는 이중 유지보수가 영구화됩니다.
다만 이건 "루미를 건드린다"의 정의에 달린 문제라 **기획·원장 판단이 필요합니다.**

---

## 5. 기획이 답해야 할 미결 사항

| # | 질문 | 왜 기획이 답해야 하나 | 막고 있는 것 |
|---|---|---|---|
| 1 | **루미/SaaS 분리 방식 A·B·C** | 제품 운영 방식의 문제 | Phase 0.5 이후 전부 |
| 2 | **판매 모듈 구성이 `BRIEF.md` 3 종 그대로인가** | 매출·비용·세무(5,100 줄)를 빼기로 했는데, `finance` 를 4 번째 모듈로 올릴지는 파일럿 이후 재검토로 미뤄둔 상태 | Phase 0.5 격리 범위 |
| 3 | **CRM 코드를 배포본에서 물리적으로 제거할 것인가** | 이 레포는 빌드 단계가 없어 정적 서빙입니다. 상수 플래그만으로는 코드가 파일에 남아 **view-source 로 경쟁 병원에 소스가 보입니다.** 빌드 제거 단계를 넣을지 결정 필요 | Phase 0.5 착수 |
| 4 | **결정 A 뒤집기 승인** (§2-2) | 신규 병원 UX 결정 | Phase 3 |
| 5 | **신규 병원 온보딩 첫 화면** (§2-3) | 현재 보류 중 | Phase 5 |
| 6 | 🆕 **비용·세무·손익·레시피의 거취** (§2-8) | 관리자앱 폐기 시 갈 곳이 없어짐. 통합앱 이관 / 폐기 / 관리자앱 유지 중 택일 | Phase 1 범위 |
| 7 | **착수 시점** | 기획이 확정되기 전에 5,000 줄을 격리하면 구성 변경 시 되돌리는 비용이 격리 비용보다 큼 | Phase 0.5 |

---

## 6. 공수·진행 현황

`AUDIT.md` 실측 기반 재추정 (1인 기준, 인일). **위 §4 의 범위 증가는 반영 전입니다.**

| Phase | 내용 | 인일 | 상태 |
|---|---|---|---|
| 0 | 조사 | — | ✅ 완료 (`AUDIT.md`) |
| HOTFIX | 재고 정합성 오류 2건 | — | ✅ 완료 (#142, #146) |
| **0.5** | CRM·FINANCE 격리, 의존성 역전, `startsWith('lumi')` 제거, 웹훅 배포 해제 | 3.5~5.5 | ⏸ **보류 (기획 대기)** |
| 1 | 데이터 모델 이관 (`clinics/{cid}/`) + `employees/{autoId}` 전환 | 8~12 | 미착수 |
| 2 | Custom Claims · 3단 게이팅 · Rules 2단계 · 테스트 인프라 | 7~11 | 미착수 |
| 3 | 하드코딩 제거 · 인센티브 v2 | 5.5~6 | 미착수 |
| 4 | 운영 콘솔 연동 (`admin.html`) | 4~6 | 미착수 |
| 5 | 온보딩 · 결제 | 8~12 | 미착수 |
| | **합계** | **약 33~48** | |

---

## 7. 참조 파일 (전부 레포에 있음)

| 파일 | 내용 |
|---|---|
| `BRIEF.md` | 원 작업 지시서 |
| `DECISIONS.md` | 1 차 회신 — 질문 4 건 답 + D-1~D-5 |
| `DECISIONS-2.md` | 2 차 회신 — HOTFIX 지시 + 결정 A/B |
| `AUDIT.md` | **Phase 0 전체 조사** — 하드코딩 인벤토리, 컬렉션 지도, CRM 결합 분석, 함수 목록, 공수 |
| `AUDIT-REPLY.md` | 착수 전 확인 3 건 회신 |
| `ISSUES.md` | 미해결 이슈 5 건 + 해결됨 3 건 + 제품 결정 기록 |
| `admin.html` | Phase 4 운영 콘솔 목업 (순수 목업, 백엔드 호출 없음) |
| `scripts/fix-currentstock.js` | 재고 정합성 조사·복구 (report/apply, 백업·멱등·테스트 19건) |

> 문서 브랜치: `claude/phase-0-inventory-audit-v3era2` — 운영 코드 0 줄, 문서만.
> 도구 브랜치: `claude/hotfix-inventory-currentstock` — 스크립트·에뮬레이터 환경.
> 둘 다 미머지이며 루미에 영향 없습니다.

---

## 8. 기획 쪽에 부탁

§5 의 6 개에 답을 주시면 개발을 재개할 수 있습니다.
**가장 급한 건 1(분리 방식)과 6(착수 시점)**입니다 — 나머지는 착수 후 순차적으로 답해도 됩니다.

그리고 §4 의 개발 속도를 감안하면, **기획 확정과 개발 착수 사이 간격이 짧을수록 좋습니다.**
간격이 길어질수록 조사 결과가 낡고 이관 대상이 늘어납니다.
