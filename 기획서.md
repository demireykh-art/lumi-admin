# 루미의원 병원 CRM 통합앱 기획서 (초안)

> 작성일: 2026-06-21
> 상태: **초안** — "내가 전달할 병원 정보" 빈칸을 채우면 상세 설계로 확정합니다.
> 목적: 기존 ERP에 **환자 단위 CRM**을 결합해 *환자입력 · 재고분석 · 매출분석 · 환자매출분석 · 지출구조 · 마케팅 의사결정*을 한 앱에서 운영.

---

## 1. 현황 분석 (AS-IS)

기존 코드는 Firebase(Firestore) + 정적 HTML/JS(모듈별 `.js`) 구조의 **단일 병원** 관리 ERP입니다. (`locations`는 별도 지점이 아니라 *5층-처치실 / 6층-시술실* 같은 **원내 보관 위치**)

### 1.1 모듈 현황

| 영역 | 상태 | 핵심 컬렉션 | 비고 |
|---|---|---|---|
| **매출** | ✅ 운영 중 | `revenue`, `salesDetail` | **월 단위 집계만** 존재 (`total, nonInsurance, japan, patients, transactions...`). 의사별/일본인스태프별 집계는 `salesDetail.doctorSales / japanStaffSales`. **환자 개인 단위 데이터 없음** ← 최대 공백 |
| **지출** | ✅ 운영 중 | `expense`, `fixedExpenses`, `variableExpenses`, `cards`, `categoryRules` | 카드내역 파싱·자동 카테고리 분류 보유 |
| **재고** | ✅ 운영 중 | `inventory`, `inventoryLogs`, `inventoryAudits`, `purchaseRequests`, `recipes` | **`recipes`가 시술명→소모품→원가를 이미 연결** (`calculateRecipeCost`). 위치별 재고 지원 |
| **인사/급여** | ✅ 운영 중 | `employees`, `attendance`, `payroll`, `leaveRequests`, `incentive*`, `*Taxes`, `mealRecords`, `lunchOT` | 근태·연차·급여·인센티브·세금 |
| **재무** | ✅ 운영 중 | `finance`, `config`, `settings` | 손익 기초 |
| **권한** | ✅ 기초 | `admins` | 역할 기반 세분화는 미흡 |

### 1.2 핵심 진단
1. **가장 큰 공백 = 환자(고객) 단위 데이터.** 현재는 "이번 달 비보험 매출 X / 환자 Y명"까지만 알 수 있고, 개별 환자의 시술·결제·재방문 이력이 없음. → **환자매출분석·마케팅 ROI는 이 데이터 없이는 불가능.**
2. **유입경로(채널) 데이터 없음.** → 채널별 CAC/ROI 산출 불가.
3. **자산은 충분.** 시술 원가(`recipes`), 지출 분류(`expense-categories`), 재고 로그가 이미 있어 환자 데이터만 채우면 분석이 빠르게 연결됨.

---

## 2. 목표 (TO-BE)

> "환자 1명을 등록·시술 기록하면 → 매출·재고·원가·마케팅 ROI가 자동으로 흐르는 통합앱"

### 2.1 6대 요구 기능 매핑

| 요구 | 구현 방향 | 의존 데이터 |
|---|---|---|
| ① 원내 환자 내용 입력 | **환자 카드 + 방문/시술 기록** 신규 모듈 (`patients`, `visits`) | 환자 입력 항목·시술 마스터 |
| ② 재고분석 | 기존 `inventory` + **시술기록→`recipes`→자동 소모품 차감/예측** | 시술↔소모품 매핑 정비 |
| ③ 매출분석 | 기존 월집계 + **환자기록 롤업**(시술별/의사별/신환·구환) | `visits` |
| ④ 환자 매출분석 | **LTV·재방문율·전환율·세그먼트** 신규 대시보드 | `patients` + `visits` |
| ⑤ 지출구조 확인 | 기존 `expense` + **매출 대비 손익/비용비중** 통합 뷰 | 비용 분류표 |
| ⑥ 마케팅 의사결정 | **채널별 비용 ↔ 유입환자 ↔ 매출** = CAC/ROAS/LTV | `channels`, 환자 유입경로 |

### 2.2 핵심 지표 (KPI) 후보
- 신환 수 / 재진율 / 휴면환자(예: 6개월+ 미방문)
- 객단가(전체/비보험/시술별), 환자 LTV
- 상담→결제 전환율
- 채널별 CAC(획득단가), ROAS(광고비 대비 매출), 채널 LTV
- 매출총이익률(매출 − 시술원가), 영업이익(− 지출)

---

## 3. 통합 데이터 모델 (신규 컬렉션 초안)

기존 컬렉션은 유지하고 아래를 추가합니다. (필드는 확정 전 초안)

```
patients (환자 마스터)   ※ 주민번호=민감정보 → §6 보안정책 적용
  { id, name, phone,
    rrnFront,          // 주민번호 앞 6자리 (생년월일 → birth/age 파생)
    rrnGender,         // 뒤 1자리 (성별·내외국인 파생)
    firstVisitAt, channelId,           // 유입경로 → 마케팅 분석 연결
    tags[], grade, memo,               // 세그먼트/VIP, 차트메모
    isJapanese, recallAt, status }     // 휴면/활성

visits (방문·시술 기록, 환자당 N건)  ← 분석의 원천
  { id, patientId, date,
    doctorId,            // 진료(창)
    assistantId,         // 어시스트(창)
    consultantId,        // 상담(창)
    ordererId,           // 오더(창)   ← 4개 담당 모두 employees와 연결
    items: [ { treatmentName, price, discount,
               qty, recipeId } ],       // recipeId로 원가·재고 차감 연결
    paid, payMethod, isNonInsurance,
    nextReservation, consultOnly }      // 상담만/결제 구분 → 전환율

treatments (시술 마스터, 정가표)        ※ 정가표 입수완료 → treatments-seed.json
  { id, category, name, note,
    isNonInsurance,
    variants: [ { product, dose, sessions,   // 제품·용량·회차
                  size, qty, region, label,   // 크기·개수·부위
                  price } ],                  // 만원 단위, VAT 별도
    recipeId }                                // 원가는 recipes(재고) 연결
  // 보톡스(제품×용량)·회차(1/3/5/10회)·크기(mm)·이벤트가 → variant로 표현

channels (마케팅 채널)  // 확정 enum: 홈페이지·블로그·소개·SNS·간판·국적
  { id, name, type,                     // paid | organic
    monthlyCost: { '2026-06': 0 } }
    // 일본마케팅(paid) 월 500만원, 국적=일본 유입과 연결 / 나머지 organic 0원

(파생) salesDetail 는 visits 롤업으로 자동 생성/대체 검토
```

연결 흐름:
`visits.items.recipeId → recipes → 소모품 자동차감(inventoryLogs) + 원가` ,
`patients.channelId → channels.monthlyCost` 로 마케팅 ROI 계산.

---

## 3.5 앱 구성 (권한 분리 — 데이터는 공유)

기존 앱이 이미 **관리자(index.html)** / **직원(staff.html)** 2개로 분리되어 같은 Firestore를 공유하는 구조. CRM도 동일 원칙을 따른다.

```
              [ 공유 Firestore ]
   patients · visits · treatments · channels  (단일 진실원천)
        ▲                                  ▲
        │ 입력 (스탭)                        │ 분석 (관리자)
   crm.html  ← NEW                      index.html (환자 탭=분석전용)
   - 직원 계정 로그인                      - bizAdmins 전용
   - 환자등록 / 방문기록 / 정가표           - 환자매출·시술별·담당자별·마케팅ROI
   - 병원 재무/급여 안 보임                 - 광고비(재무) 입력은 여기서만
```

- **데이터(컬렉션)는 하나로 공유**해 "통합 CRM"을 실현하되, **화면(접근권한)만 분리**.
- 스탭은 crm.html에서 환자 입력만 → 병원 매출/지출/급여는 노출 안 됨.
- 관리자는 index.html에서 같은 데이터를 읽어 분석 + 광고비 입력.

## 4. 화면(IA) 초안

```
1. 대시보드 (오늘/이번달 KPI 한눈에: 신환·매출·전환율·채널ROAS)
2. 환자
   2-1. 환자 목록/검색         (등록·태그·휴면 필터)
   2-2. 환자 상세 카드          (방문이력·LTV·다음예약)
   2-3. 방문/시술 입력          (★ 핵심 입력 화면)
3. 매출분석   (시술별/의사별/신환·구환/객단가 추이)
4. 환자매출분석 (LTV·재방문율·전환율·세그먼트)
5. 재고        (현황 · 자동차감 · 발주예측 · 레시피)   ← 기존 확장
6. 지출/손익   (비용구조 · 매출대비 비중 · 영업이익)   ← 기존 통합
7. 마케팅      (채널별 비용·유입·CAC·ROAS·LTV)
8. 인사/급여   (기존 유지)
9. 설정/권한   (역할별 데이터 접근)
```

---

## 5. 개발 로드맵 (제안)

| 단계 | 내용 | 산출물 |
|---|---|---|
| **0. 정보수집** | §7 체크리스트 회수, 본 기획서 확정 | ✅ 완료 (기획서·treatments-seed.json) |
| **1. 환자 CRM 기반** | 권한분리(스탭 입력앱 / 관리자 분석) + 공유 데이터 모델 | ✅ 완료 (스탭 `crm.html`+`patients.js`, 관리자 `crm-analytics.js` 분석전용) |
| **2. 자동 연동** | 시술기록 → 재고 자동차감 + 원가 반영 | 진행 예정 |
| **3. 분석 대시보드** | 매출/환자매출 분석 | 분석 화면 |
| **4. 마케팅·손익** | 채널 ROI + 손익 통합 뷰 | 의사결정 대시보드 |
| **5. 권한·보안** | 역할별 접근, 환자정보 접근로그 | 운영 안정화 |

---

## 6. 정책·리스크
- **주민번호(고유식별정보) 취급**: 입력하시는 *앞6+뒤1*은 개인정보보호법상 **민감/고유식별정보**. 권장 처리방식 — ① 화면엔 마스킹(예: `900101-1******`) ② 저장 시 **암호화** ③ 분석엔 생년월일·성별·연령대만 **파생값**으로 사용(원본 직접조회 최소화) ④ 조회·수정 **접근로그** 기록.
- **개인정보(환자정보)**: 접근권한 분리·접근로그·최소수집 원칙 필요. 의료법/개인정보보호법 검토.
- 기존 월집계 매출과 신규 환자기록 롤업 간 **정합성**(과거 데이터 마이그레이션 범위) 결정 필요.

---

## 7. ★ 내가(병원이) 전달할 정보 체크리스트

> 아래를 채워주시면 §3 데이터모델·§4 화면을 확정합니다. 실제 데이터는 **가명처리 샘플 일부**면 충분합니다.

### A. 병원 기본
- [ ] 진료과목 / 주력 시술 항목:
- [ ] 보험 : 비보험 매출 비중 (대략 %):
- [ ] 외국인(일본인 등) 환자 비중 / 별도 관리 필요 여부:

### B. 환자 입력 항목  ✅ 확정
- [x] 환자 카드 항목: **이름 / 주민번호(앞6 + 뒤1) / 전화번호 / 유입경로**
- [x] 방문 1건당 담당자 4종: **진료 · 어시스트 · 상담 · 오더** → **방문마다 다름 → `visits` 단위 저장**
- [x] 방문기록에 **시술명 · 금액 · 할인 · 결제수단 · 다음예약** 전부 입력 → 매출·객단가·전환율 분석 연결
- [ ] (확인필요) 리콜·재방문 관리, 동의서·사진 보관 필요 여부

### C. 시술·상품 마스터  ✅ 입수완료 → `treatments-seed.json`
- [x] **정가표 구조화 완료**: 18개 카테고리 / 98개 시술 / 194개 가격변형 (만원·VAT별도)
- [x] 원가: **admin 재고내역(`recipes`)에서 산출** — 별도 입력 불필요
- [x] 컴비네이션(복합) 시술의 구성 재료는 `recipes`에 **입력 필요**
- [x] **돌출점**: 크기 구간 2종(구간A/B) 모두 운영으로 확정
- [x] **전 시술 비보험만 운영** 확정 (`_meta.isNonInsurance:true`)
- [ ] (원장 확인필요) **청색점 절개술** 가격 구간 표기만 모호 → `reviewNeeded:true` 유지

### D. 매출분석
- [ ] 우선 보고 싶은 분석 축 (시술별/의사별/실장별/유입경로별/신환·구환…):
- [ ] 월·연 목표 매출(KPI):

### E. 환자매출분석
- [ ] 보고 싶은 지표 (LTV·재방문율·전환율·객단가분포…):
- [ ] 휴면환자 기준(개월), VIP/등급 기준:

### F. 지출구조
- [ ] 현재 비용 분류표 (기존 `expense-categories` 기준 + 보완):
- [ ] 손익 화면 단위(월별/시술별), 영업이익 정의:

### G. 마케팅  ✅ 확정
- [x] **유입경로 선택지(환자등록 시): 홈페이지 · 블로그 · 소개 · SNS · 간판 · 국적**
- [x] 유료 채널: **일본 마케팅(마케이션) 월 500만원** (국적=일본 유입과 연결) — 그 외 유료비용 없음
- [x] organic(비용 0): **홈페이지 · 블로그 자체작성 · 직원 SNS · 소개 · 간판**
- [x] 채널 ROI 산식: 일본마케팅 CAC = 500만 ÷ 신규 일본환자수, ROAS = 일본환자 매출 ÷ 500만

### H. 운영·권한
- [x] **CRM 접근권한을 관리자(index.html 설정탭)에서 제어** — 전 직원 / 선택 직원, `settings/crmAccess`
- [x] **로그인 직원 = 차팅 작성자**로 기록 (`createdBy`/`chartedBy`), 방문이력에 "기록:이름" 표시
- [x] 로그인 계정: 직원관리에서 로그인ID 부여 시 생성 (대표원장도 직원 등록 후 권한 부여)
- [ ] (향후) 환자정보 조회·수정 **접근로그** 적재, 주민번호 암호화 (5단계 보안)
