# 할증(덤) 처리 — 루미 재고에 적용하기

- 작성 2026-09-10 · 출처 `jaegodoc` (재고닥) `functions/lib/invoice.js` · `public/calc.js`
- 관련 `RESEARCH.md` §A(루미 단가) · §D-5-5(할증)

---

## 0. 한 줄

**할증은 「금액을 깎아 주는 것」이 아니라 「수량을 더 주는 것」이다.**
그래서 단가의 **분모**를 고쳐야 하고, 분자(금액)는 건드리지 않는다.

```
실취득 단가 = 실제 낸 돈 ÷ (유상 수량 + 무상 수량)
```

---

## 1. 무엇이 문제인가

거래처가 「10박스 사면 5박스 더」를 줍니다. 명세서에는 **두 줄**로 옵니다.

```
TXA NIA        수량 10   단가 220,000   금액 2,200,000
TXA NIA        수량  5   단가       0   금액         0     ← 할증
```

실제로는 **2,200,000원에 15박스**를 산 것입니다.

| | 계산 | 결과 |
| --- | --- | ---: |
| 인쇄된 단가 | 2,200,000 ÷ 10 | 220,000 |
| **실취득 단가** | 2,200,000 ÷ **15** | **146,667** |
| 낱개 (1박스 5병) | 146,667 ÷ 5 | 29,333 |

**33% 차이입니다.** 이 물건을 쓰는 시술의 원가가 33% 부풀려집니다.

---

## 2. 지금 루미는 어떻게 되어 있나

단가를 쓰는 곳이 세 군데이고, **셋 다 같은 식**입니다.

| 위치 | 함수 | 식 |
| --- | --- | --- |
| `staff.html:15459` | `saveNewItem()` | `unitPrice = totalPrice / (boxes × perBox)` |
| `staff.html:15645` | 재고탭 입고 | 같은 식 · `unitPrice` **덮어쓰기** |
| `staff.html:16140` | 품목 수정 | 같은 식 |
| `staff.html:16852` | 입고탭 | 단가를 **안 건드림** |

### 사실은 표현할 수 있습니다 — 다만 아무도 모릅니다

직원이 `boxes` 에 **15**(유상 10 + 할증 5)를, `totalPrice` 에 **2,200,000**을 넣으면
지금 식으로도 146,667이 나옵니다. 식이 틀린 게 아닙니다.

**문제는 그렇게 넣으라고 말해 주는 곳이 없다는 것입니다.** 명세서에 「수량 10」이
크게 찍혀 있으니 10을 넣는 게 자연스럽고, 그러면:

| 직원이 넣은 것 | 단가 | 재고 |
| --- | --- | --- |
| 박스 10 / 금액 2,200,000 | **220,000 (33% 과대)** | **5박스 부족** |
| 박스 15 / 금액 2,200,000 | 146,667 ✅ | 15박스 ✅ |
| 박스 15 / 금액 3,300,000 | 220,000 (과대) | 15박스 ✅ |

같은 명세서를 두 사람이 넣으면 다른 값이 저장됩니다. 그리고 **저장된 뒤에는
어느 쪽으로 넣었는지 알 방법이 없습니다.**

### 그 밖에 못 하는 것

1. **전액 무상을 표현할 수 없다.**
   장비를 사면 소모품을 무상으로 주고, 그 명세서에는 금액 칸이 아예 비어 있습니다
   (제이시스메디칼). `totalPrice = 0` → `unitPrice = 0` →
   **그 물건을 쓰는 시술의 원가가 0원으로 나옵니다.**

2. **할증이 있었다는 사실이 안 남는다.** 나중에 단가가 왜 이런지 못 따집니다.

3. `unitPrice` 를 **덮어씁니다**(`:15645`). 이동가중평균이 아니라 최종매입가입니다.
   이건 할증과 별개 주제이지만, 같은 줄을 고칠 때 같이 봐야 합니다 (`RESEARCH.md` §A).

---

## 3. 재고닥이 하는 방식

세 단계로 나뉩니다. **순서가 중요합니다.**

### ① 병합 — 같은 물건의 두 줄을 한 줄로

`functions/lib/invoice.js` `mergeFreeGoods()`

같은 품목의 유상 행과 무상 행을 합칩니다. 「같은 품목」은 **거래처 품목코드**로
판정하고, 코드가 없으면 `정규화한 이름 + 규격`으로 판정합니다.

```js
const key = normalizeCode(l.vendorCode) || normalizeName(l.name) + '|' + String(l.spec || '');
const paid = num(l.unitPrice) > 0;      // 단가 0 = 무상분

if (paid) {
  cur.qtyPaid += num(l.qty);
  cur.gross   += num(l.amount) || num(l.unitPrice) * num(l.qty);
} else {
  cur.qtyFree += num(l.qty);            // ★ 수량만 더한다. 금액은 안 건드린다
}
...
l.qty = l.qtyPaid + l.qtyFree;
```

**핵심은 마지막 줄입니다.** 무상분은 `qtyFree` 에만 쌓이고 금액에는 한 푼도
더해지지 않습니다. 그래서 합친 뒤 `금액 ÷ 수량`을 하면 자동으로 할증이 반영됩니다.

### ② 단가 — 분모가 유상+무상

`public/calc.js` `computeReceipt()`

```js
const qtyPacks = paidPacks + freePacks;          // ★ 분모
const qtyBase  = qtyPacks * packSize;

const basePricePack = isFree ? refPack : (qtyPacks > 0 ? Math.round(amount / qtyPacks) : 0);

return {
  qtyPacks, qtyBase,
  amount: isFree ? 0 : amount,
  unitPricePack: basePricePack,
  unitPriceBase: Math.round(basePricePack / packSize),
  // 명세서에 인쇄됐을 단가 — 화면 대조용으로만 쓴다
  printedUnitPricePack: (!isFree && paidPacks > 0) ? Math.round(amount / paidPacks) : 0,
  hasFreeGoods: freePacks > 0,
  costSource: isFree ? 'reference' : 'paid',
};
```

`printedUnitPricePack` 를 따로 두는 이유: 화면에 **`인쇄 ₩220,000 → ₩146,667`** 로
나란히 보여 주기 위해서입니다. 사람이 명세서와 대조할 때 인쇄된 숫자가 안 보이면
「내가 잘못 넣었나」 싶어 되돌립니다.

### ③ 전액 무상 — 지불액과 원가를 분리

`amount` 와 `unitPrice` 를 **따로** 저장합니다. 하나로 묶으면 둘 중 하나가 거짓이 됩니다.

| | 값 | 무엇이 맞아지는가 |
| --- | --- | --- |
| `amount` | **0** | 매입액·현금흐름 (사실) |
| `unitPrice` | **참고 단가** (사람이 입력) | 재고자산·시술원가 (쓸모) |
| `costSource` | `'reference'` | 이 원가가 추정치임을 표시 |

원가를 0으로 두면 그 물건을 쓸 때 시술 원가가 공짜로 나옵니다.
금액을 참고가로 채우면 사지도 않은 돈이 매입액에 잡힙니다. **둘 다 틀립니다.**

### ④ 안분은 다른 축이다 — 헷갈리지 말 것

주문할인·배송비는 **금액**을 건드리고, 할증은 **수량**을 건드립니다.

```js
// allocate() — 금액 조정을 품목별 금액 비율로 나눈다
const share = (i < out.length - 1) ? Math.round(amt * num(l.gross) / basis) : amt - acc;
```

마지막 행에 `amt - acc` 를 주는 것은 **반올림 잔돈이 사라지지 않게** 하기 위해서입니다.
비율로만 나누면 1원이 남거나 모자라고, 그러면 검산이 매번 깨집니다.

> 할증과 안분은 같은 명세서에 함께 올 수 있고, 순서는 **병합 → 세포함 환산 → 안분 → 단가**입니다.
> 안분을 병합보다 먼저 하면 무상 행에도 할인이 나뉘어 들어갑니다.

---

## 4. 루미에 넣는 최소 변경

### 4-1. 입력칸

지금 「박스 수」 하나인 자리를 둘로 나눕니다.

```
  유상 수량 [ 10 ] 박스      할증(덤) [ 5 ] 박스
  지불 금액 [ 2,200,000 ] 원
  → 15박스 (×5 = 75병) · 박스당 ₩146,667 · 낱개 ₩29,333
    인쇄 단가 ₩220,000 → 할증 반영 ₩146,667
```

- **할증 칸의 기본값은 0** 입니다. 대부분의 입고는 할증이 없으므로 평소 화면은 지금과 같습니다.
- 도움말 한 줄: *「명세서에 단가 0으로 찍힌 덤은 여기에 넣으세요. 단가가 그만큼 내려갑니다.」*

### 4-2. 저장 필드

`inventory` 문서에 세 개를 더합니다. 기존 필드는 **뜻을 바꾸지 않습니다.**

| 필드 | 뜻 | 비고 |
| --- | --- | --- |
| `purchaseQtyPaid` | 유상 수량(낱개) | 새로 |
| `purchaseQtyFree` | 무상 수량(낱개) | 새로 · 없으면 0 |
| `costSource` | `'paid'` \| `'reference'` | 새로 |
| `purchaseQty` | **유상 + 무상** | 뜻 그대로 (합계) |
| `purchasePrice` | 실제 낸 돈 | 뜻 그대로 |
| `unitPrice` | `purchasePrice / purchaseQty` | 식 그대로 |

**`unitPrice` 계산식은 한 글자도 안 바뀝니다.** `purchaseQty` 에 무상분이 포함되도록
입력을 바꾸는 것뿐입니다. 그래서 기존 데이터·화면·리포트가 그대로 돕니다.

### 4-3. 고칠 자리

| 위치 | 무엇을 |
| --- | --- |
| `staff.html:15459` `saveNewItem()` | `totalQty = boxes × perBox` → `(paidBoxes + freeBoxes) × perBox` |
| `staff.html:15645` 재고탭 입고 | 같은 변경 + `priceHistory` 에 `qtyFree` 같이 기록 |
| `staff.html:16140` 품목 수정 | 같은 변경 |
| `staff.html:15421` `calcNewItemUnit()` | 미리보기 식도 같이 (안 맞으면 사람이 혼란) |
| `staff.html:16852` 입고탭 | 단가를 안 건드리므로 **그대로 둡니다** |

### 4-4. 전액 무상

「유형」을 하나 추가합니다.

```
  ○ 구매 — 돈을 냈습니다
  ○ 무상 제공 — 장비에 딸려 왔습니다
```

무상 제공을 고르면 금액 칸이 「참고 단가」로 바뀝니다.

```js
purchasePrice: 0,
unitPrice:     refUnitPrice,     // 사람이 입력
costSource:    'reference',
```

목록에서 `costSource === 'reference'` 면 단가 옆에 「추정」을 붙입니다.
**추정치를 실측치처럼 보여 주면 나중에 아무도 구분하지 못합니다.**

---

## 5. 검산 — 실제 숫자로

프라이모리스 Order (골든셋 ③)

```
입력   유상 10박스 · 할증 5박스 · 지불 2,200,000원 · 1박스 5병

purchaseQtyPaid  = 10 × 5 = 50
purchaseQtyFree  =  5 × 5 = 25
purchaseQty      =         75
purchasePrice    =  2,200,000
unitPrice        =  2,200,000 / 75 = 29,333   ← 낱개
박스당            =  2,200,000 / 15 = 146,667
인쇄 단가         =  2,200,000 / 10 = 220,000  (화면 대조용)
```

**⚠️ 여기서 `unitPrice` 를 146,667(박스당)로 저장하면 안 됩니다.**
루미의 `unitPrice` 는 **낱개당**이고 `currentStock` 도 낱개입니다.
박스가를 낱개 칸에 넣은 것이 `RESEARCH.md` §A-5(3) 의 **₩8,213,380 과대 계상** 사고입니다.

```
잘못  70병 × 146,667 = 10,266,690
바름  70병 ×  29,333 =  2,053,310
```

할증 칸을 새로 만들면서 같은 실수를 반복할 위험이 가장 큰 지점입니다.

---

## 6. 하지 말아야 할 것

| 하지 말 것 | 왜 |
| --- | --- |
| 무상분을 **단가 0인 별도 품목**으로 등록 | 재고가 둘로 갈라지고, 출고할 때 어느 쪽을 뺄지 아무도 모릅니다 |
| 무상분을 **재고에서 빼고** 단가만 맞추기 | 실물은 창고에 있는데 장부에 없습니다. 실사 때마다 어긋납니다 |
| 무상 제공의 원가를 **0원**으로 | 그 물건을 쓰는 시술이 공짜로 나옵니다 |
| 무상 제공의 금액을 **참고가**로 | 사지도 않은 돈이 매입액에 잡힙니다 |
| 할증을 **금액 할인**으로 환산해서 넣기 | 수량이 안 맞습니다. 할증은 수량 축, 할인은 금액 축입니다 |
| `unitPrice` 를 아무 방어 없이 덮어쓰기 | 박스가/낱개가를 섞으면 조용히 배수로 틀립니다 (§5) |

---

## 7. 같이 넣으면 좋은 방어 한 가지

재고닥의 `Calc.checkPackSize()` 는 **총액으로 역산해서** 단가가 아는 값에서
크게 벗어나면 막습니다. 할증 칸을 만들면서 같이 넣으면 §5의 사고를 잡습니다.

```js
const implied = 지불금액 / 총수량;          // 이번 입고의 낱개 단가
const ratio   = implied / 기존_unitPrice;   // 아는 값과 비교

if (ratio >= 5 || ratio <= 0.2) {
  // "낱개 단가가 아는 값(29,333원)의 5배입니다.
  //  박스 입수가 1이 아니라 5가 아닐까요?"
}
```

임계 5배/⅕은 실제 가격 변동을 오탐하지 않을 만큼 넓게 잡은 값입니다.
**전에 산 적이 있는 품목에만 쓸 수 있습니다.** 첫 입고는 사람이 봐야 합니다.

---

## 8. 참고 파일

| 파일 | 무엇 |
| --- | --- |
| `jaegodoc/functions/lib/invoice.js` | `mergeFreeGoods()` · `allocate()` |
| `jaegodoc/functions/test/invoice.test.js` | 골든셋 실제 숫자로 된 시험 |
| `jaegodoc/public/calc.js` | `computeReceipt()` · `checkPackSize()` |
| `jaegodoc/tests/calc.test.js` | 같은 로직의 시험 |
| `RESEARCH.md` §A | 루미 단가 로직 현황과 4개 구조적 위험 |
| `RESEARCH.md` §D-5-5 | 할증 설계 근거 |
