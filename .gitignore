node_modules/
functions/node_modules/
functions/package-lock.json
.firebase/
firebase-debug.log
firebase-debug.*.log
*.log
.env
.env.local

# fix-currentstock.js 백업 덤프 — 실데이터. 커밋 금지
backup/

# 에뮬레이터 스냅샷 — 실데이터. 커밋 금지
snapshot/
firestore-debug.log

# 서비스 계정 키 — 절대 커밋 금지 (레포 밖에 두는 것이 원칙, 아래는 2차 방어선)
serviceAccount*.json
service-account*.json
*-adminsdk-*.json
*.credentials.json
gcp-key*.json

# fix-currentstock.js report --out 산출물
report*.json

# 재고 단가 조사 보고서 — 실데이터. 커밋 금지
price-audit.*

# 재고닥 부트스트랩 키트 — 새 레포로 옮길 짐. 의존성은 커밋하지 않는다
jaegodak-bootstrap/node_modules/
jaegodak-bootstrap/.firebase/
